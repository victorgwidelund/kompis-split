# Kompis Split – levande projektkontext

Senast uppdaterad: 2026-09-01
Appversion: 1.29.1
Databasschema: migration 12

Det här dokumentet är den korta tekniska minnesbilden för framtida utveckling. Det ska uppdateras i samma ändring när arkitektur, datamodell, drift, säkerhet, viktiga funktioner, releaser eller kända problem förändras. Lägg aldrig in lösenord, tokens, privata nycklar, riktiga telefonnummer, kvitton eller andra personuppgifter här.

## Produktmål

Kompis Split är en privat, självhostad app för en mindre vänkrets. Den ska göra det enkelt att:

- dela utgifter på resor och behålla en tillförlitlig historik över skulder och betalningar;
- bjuda in vänner och återanvända registrerade kontakter;
- använda snabbnota för att bocka av individuella kvittorader i realtid;
- läsa svenska kvitton lokalt med OCR och alltid låta användaren kontrollera förslagen;
- se enkel statistik utan att göra gränssnittet onödigt komplext.

Swish är ännu inte integrerat. Endast dokumenterade Swish-funktioner får införas, och ett öppnat Swish-flöde får aldrig räknas som bekräftad betalning.

## Nuvarande arkitektur

- Frontend: React 19, Vite och strikt TypeScript i `frontend/src/`, uppdelad i `api`, `components`, `features`, `types` och `utils`.
- Backend: Node.js 24 och TypeScript i `src/`.
- Databas: PostgreSQL 17, endast tillgänglig inne i Compose-nätverket.
- Databasåtkomst: `pg` med frågeadapter i `src/database.ts`.
- Migreringar: ordnade, framåtriktade migreringar i `src/migrations.ts`, registrerade i `schema_migrations`.
- OCR: en persistent CPU-tjänst med RapidOCR 3.9.2/ONNX Runtime, PP-OCRv6-small-detektor och PP-OCRv5 Latin-mobile-igenkännare. Bounding boxes och OCR-säkerhet går till en svensk kvittoparser med typat nullable schema, heltalsöre, källevidens och deterministisk avstämning. Tesseract.js är lokal driftreserv; ingen VLM eller extern tjänst ingår i produktionsvägen.
- Realtid: Server-Sent Events för snabbnota.
- Pakethanterare: pnpm 11.16.0.
- Produktion: Docker Compose på Unraid och publicerad image i GitHub Container Registry.
- Statisk leverans: Vite bygger hashade assets. Samma Node-process som tidigare serverar bygget, så port, reverse proxy, sessionscookies och appcontainer är oförändrade.

Bevara denna arkitektur om inte en större förändring uttryckligen har godkänts och motiverats.

## Produktionstopologi och betrott proxy-läge

Quick Scan ska nås via LAN/VPN eller DNS-only → Nginx Proxy Manager → appcontainern. En
Cloudflare-proxied/CDN-väg tar emot requestkroppen och får därför inte användas när kvittot ska
stanna på egen infrastruktur. Se
`DEPLOYMENT.md` för den fullständiga topologin och de operativa flödena
(uppdatering, backup, återställning, rollback).

- Klient-IP för rate limiting (inloggning, gäst-anslutning till Snabbnota,
  kvittoanalys) hämtas från `CF-Connecting-IP` när `TRUST_PROXY=true` —
  aldrig från `X-Forwarded-For`s första värde, eftersom både Cloudflare och
  Nginx lägger till i den headern och en klient fritt kan sätta ett eget
  förstaled. `CF-Connecting-IP` sätts av Cloudflares edge utifrån den
  faktiska anslutningen och går inte att förfalska av klienten.
- `X-Forwarded-Host`/`X-Forwarded-Proto` används fortfarande för
  absoluta inbjudningslänkar och Origin-verifiering; det förutsätter att
  Nginx Proxy Manager skriver över (inte bara vidarebefordrar) dessa
  headrar. `SameSite=Strict` på sessionscookien är det huvudsakliga
  CSRF-skyddet oavsett proxykonfiguration.

## Drift på Unraid

- Compose-stacken ligger i `compose.yaml`.
- App: `ghcr.io/victorgwidelund/kompis-split:latest`, port 8787.
- PostgreSQL: `postgres:17.10-alpine3.22`, utan publicerad host-port.
- Beständig databas: `/mnt/user/kompis_split/postgres`.
- Automatiska dump-backuper: `/mnt/user/kompis_split/backups`, normalt 14 dagars retention.
- Lokal inference: `ghcr.io/victorgwidelund/kompis-split-receipt-inference:latest`, utan hostport, på ett `internal: true`-nät. Modellerna är 17,6 MiB, ligger checksummekontrollerade i imagen och kräver ingen GPU eller persistent modellvolym.
- Appcontainern är read-only, saknar Linux capabilities och använder `/tmp` som begränsad tmpfs.
- Produktionsåtkomst ska gå via HTTPS-reverse proxy. `COOKIE_SECURE=true` och `TRUST_PROXY=true` används när proxyn är korrekt konfigurerad.
- Hemligheter sätts i Unraid Compose Manager eller ignorerad `.env`, aldrig i Git.

Radera eller återskapa aldrig databasvolymen vid en vanlig uppdatering. En imageuppdatering ska normalt göras med Pull & Up i Compose Manager. Behåll föregående `sha-*`-tagg för rollback.

## Identitet och behörighet

- Vanliga appfunktioner kräver konto och en opaque, utgående HttpOnly-session.
- Servern kontrollerar adminroll, resmedlemskap och ägarroll för varje skyddad operation.
- Vanliga vän- och reseinbjudningar kräver registrering eller inloggning.
- Snabbnota har ett begränsat gästläge: namn och mobil-/Swishnummer räcker.
- En snabbnotegäst får en separat HttpOnly-session som endast gäller en bestämd snabbnota och inte ger åtkomst till dashboard, resor, statistik eller administration.
- Inbjudnings- och sessionstokens lagras hashade, inte i klartext.
- Snabbnotans ägare kan se gästens nummer; övriga deltagare ser endast namnet.
- `/api/users/search` returnerar aldrig telefonnummer — bara namn/e-post för att hitta rätt person att bjuda in. Telefonnummer blir synliga för andra användare först när man faktiskt delar en resa eller snabbnota (deltagarlistan), aldrig via fritextsök.
- Att spara någon som kontakt (`POST /api/contacts`) kräver att man redan delar en resa eller snabbnota med personen. Ett gissat eller uppräknat användar-ID räcker inte för att läsa ut någons e-post/Swish-nummer den vägen.
- Lösenordsåterställning (`POST /api/auth/forgot-password` → `POST /api/auth/reset-password`) svarar alltid identiskt oavsett om adressen finns, för att inte avslöja registrerade konton. Återställningstoken lagras hashad (samma mönster som inbjudningar), gäller i 30 minuter, är engångsgiltig och en lyckad återställning loggar ut kontot från alla enheter (`DELETE FROM sessions`).

## E-post (Microsoft Graph)

- E-postavsändning sker app-only via Microsoft Graph (`src/email.ts`), inte SMTP (fasas ut på Exchange Online). Inställningarna (tenant-ID, klient-ID, klienthemlighet, avsändaradress) lagras i en enda rad i `email_settings` (migration 10) och konfigureras under Administration → E-post, inte via miljövariabler — klienthemligheten returneras aldrig av `GET /api/admin/email-settings`, bara `hasSecret: true/false`.
- Se `scripts/setup-email-integration.ps1` och `scripts/scope-email-sender.ps1` för hur själva Entra ID-app-registreringen och behörigheten skapas i Microsoft 365-tenanten (körs av användaren själv, inte av appen).
- Tre användningar hittills: lösenordsåterställning, ett admin-testmail (`POST /api/admin/email-settings/test`) och betalningspåminnelser (`POST /api/remind-unpaid`, tillgängligt för alla inloggade konton — inte bara admin). Skickar ett samlat mail per person som är skyldig *den inloggade användaren* pengar — beräknat från resors avräknade skulder (`simplifyDebts`) där man själv är fordringsägare och obetalda snabbnotaandelar för snabbnotor man äger, aldrig till snabbnotagäster (de saknar konto/e-post) och aldrig om någon annans skuld till en tredje part.

## Admin-only demoläge

- Endast en global admin kan gå in i demoläge (`POST /api/admin/demo/enter`), verifierat server-side. Ett vanligt konto får 403 även vid direkt API-anrop.
- Demoläget är bundet till sessionsraden i databasen (`sessions.demo_mode`, `sessions.demo_batch_id`), inte till något klientskickat fält och inte till `localStorage`. Klienten läser bara `demoMode` från `/api/session`.
- Vid första `enter` skapas en engångsuppsättning fiktiv data (två resor, en snabbnota) taggad med `is_demo = TRUE` och sessionens `demo_batch_id`, via samma tabeller, frågor och React-komponenter som vanliga resor — ingen parallell demo-frontend.
- Isoleringen upprätthålls på ett enda ställe per resurstyp: `requireAccess`, `quickTabAccess` och `loadTrip` jämför resans/snabbnotans `is_demo`-flagga mot sessionens aktuella läge (via request-scoped `AsyncLocalStorage`, samma mönster som transaktionshanteringen i `src/database.ts`) och nekar annars — en demo-session kan alltså aldrig läsa en riktig resa och en vanlig session kan aldrig läsa en demo-resa, oavsett `trip_access`. `dashboard`, `statistics` och listan över snabbnotor filtrerar på samma flagga. Riktiga kontakter returneras aldrig i dashboarden i demoläge.
- En liten uttrycklig nekningslista (samma ställe i `handleApi`) blockerar `/api/users/search`, `/api/contacts`, `/api/admin`, `/api/admin/users/:id` samt alla inbjudningsskapande endpoints medan `demo_mode` är sant — dessa data/åtgärder är antingen globala (inte resescopade) eller skulle exponera riktiga kontaktuppgifter.
- Resor/snabbnotor som skapas *under* pågående demoläge (t.ex. admin klickar "Skapa resa" för att visa flödet) taggas automatiskt med samma `is_demo`/`demo_batch_id` och städas bort tillsammans med resten.
- `POST /api/admin/demo/exit` raderar hela batchen (`DELETE FROM trips/quick_tabs WHERE demo_batch_id = ?`, kaskad tar hand om deltagare/utgifter/kvitton/claims) och återställer sessionen — riktig kontext är tillbaka omedelbart, ingen ny inloggning krävs. En bakgrundsstädning (var 60:e minut, plus vid start) tar bort demodata vars session gått ut, om en admin stänger fliken utan att klicka "Avsluta demoläge".
- Kategorier är globala, inte resescopade, så `POST/PATCH /api/categories` blockeras explicit i demoläge (den centrala nekningslistan räcker inte där eftersom `GET /api/categories` måste fungera för att demoresorna ska kunna visa kategorier).
- Frontend: en banderoll ("DEMOLÄGE – …") visas när `session.demoMode` är sant, och en "Demoläge"-knapp finns i adminvyn. Att gå in/ur laddar om sidan (`location.reload()`) i stället för att tråda `demoMode` genom varje komponent.

## Ekonomi och data

- Alla belopp lagras och beräknas som heltalsöre. Historiska kolumnnamn med `_cents` betyder fortfarande öre.
- Delningar måste vara deterministiska och summan av andelarna måste alltid vara exakt lika med originalbeloppet.
- Utgifter och betalningar är huvudboken. Saldon beräknas från den och lagras inte som separat sanning.
- Finansiella poster mjukraderas eller reverseras så att historiken bevaras.
- Resor kan arkiveras och mjukraderas. Kvittofiler kopplade till en borttagen resa tas bort enligt appens nuvarande dataskyddsbeteende.
- OCR-resultat är redigerbara förslag, aldrig en auktoritativ tolkning av kvittot. RapidOCR ger lokal text/geometri/säkerhet, parsern bevarar källevidens och radsummor/rabatter valideras i heltalsöre mot totalen. Avvikelser och svag evidens markeras för extra kontroll; okända fält lämnas null.

## Databasmigreringar

Aktuell högsta version är 12:

1. Grundschema för användare, resor, utgifter, betalningar, åtkomst och audit-logg.
2. Global adminroll, möjlighet att inaktivera konton och frivilligt utgiftsdatum.
3. Resepapperskorg, kategorier, kvittofiler och väninbjudningar.
4. Fristående snabbnota med kvittorader, inbjudningar och registrerade deltagare.
5. Kontolösa snabbnotegäster och claims som kan tillhöra antingen användare eller gäster.
6. Antal per snabbnoterad och per deltagares val, utan att samma produkt behöver delas upp i flera rader.
7. Admin-only demoläge (`is_demo`/`demo_batch_id` på `trips` och `quick_tabs`, `demo_mode`/`demo_batch_id` på `sessions`) samt en korrigering av tre främmande nycklar mot `participants(id)` (`expense_shares.participant_id`, `expenses.payer_id`, `payments.from_id`/`to_id`) som saknade `ON DELETE CASCADE` — upptäcktes eftersom demolägets städning är första koden som någonsin hårdraderar en resa.
8. Betalningsspårning för snabbnotor (`quick_tab_payments`), tillitsbaserad självrapportering precis som resornas "Markera betald".
9. Buggrapporter (`bug_reports`) med beskrivning, sidkontext, breadcrumbs och valfri skärmbild.
10. E-postinställningar (Microsoft Graph, en rad) och lösenordsåterställningstoken.
11. Fritextkommentarer på en utgift (`expense_comments`), synliga för hela resan oavsett redigeringsbehörighet.
12. Valfri profilbild (`users.avatar_mime_type`/`avatar_content`), lagrad som bytes precis som kvitton och buggrapportsskärmdumpar.

Nya schemaändringar ska få nästa migrationsnummer. Ändra inte en redan distribuerad migration och gör inga destruktiva volymoperationer.

## Viktiga nuvarande funktioner

- Svenska konton, sessionsinloggning och adminvy.
- Startsida med resor, utgifter och kontakter.
- Skapa, arkivera, återställa och mjukradera resor.
- Skapa och redigera utgifter med flera delningsmetoder.
- Kategorier, kvittouppladdning och statistik.
- Vän-, rese- och snabbnoteinbjudningar med länk och QR-kod.
- Svensk OCR för belopp, datum, handlare och kvittorader.
- Snabbnota med registrerade användare eller kontolösa gäster, individuella claims och realtidsuppdatering.
- Snabbnoter behåller produktmängder på en gemensam rad, låter varje deltagare välja antal och erbjuder förifylld Swish-betalning till skaparen.
- Kvittobilder komprimeras i webbläsaren (mål ~3200 px långsida, JPEG ~0,85) innan uppladdning och normaliseras igen med Sharp på servern innan lagring, oavsett vad klienten skickade.
- Admin-only demoläge: en admin kan gå in i en isolerad, fiktiv datauppsättning (två resor, en snabbnota) utan att någonsin kunna läsa eller ändra riktiga användares data, och lämna läget igen utan att logga ut.
- Valfri profilbild per konto (Mer-sidan), normaliserad och lagrad som bytes; faller tillbaka till initialer överallt annars.
- Live kategoriförslag utifrån utgiftens titel när en ny utgift skapas, tills användaren själv väljer en kategori.
- Synligt förenklat versionsnummer från `package.json`/`APP_VERSION`.
- I en resas "Utgifter"-flik: filter mellan alla utgifter och bara de den inloggade själv är tillagd i, samt en liten "Din andel"-etikett per rad.

## Test- och releaseflöde

Krav före release:

1. `pnpm run typecheck`
2. `pnpm run lint`
3. `pnpm test`
4. `docker compose config --quiet` där Docker finns
5. Grön GitHub Actions-körning med PostgreSQL-integrationstest och containerbygge

En push till `main` bygger och publicerar `latest` samt en oföränderlig `sha-*`-tagg. Pull requests kör tester, bygger appcontainern och validerar Compose men publicerar inte produktionsimagen.

## Senaste utvecklingsstatus

- Version 1.29.1 rättar tre parser-buggar som alla föll ut ur **ett** verkligt restaurangkvitto (Göteborg, aug 2026) en användare rapporterade efter en Snabbnota-skanning. Två felspår uteslöts först med mätning i stället för antagande, båda värda att inte upprepa: **(a)** kvittot såg 90°-roterat ut i rapporten, men fotot bär EXIF-orientering 6 och hela kedjan hanterar det redan (`sharp.rotate()` i `prepareReceiptImages`, `ImageOps.exif_transpose` i `receipt-inference/app.py`) — visningsprogrammet ignorerade EXIF, appen gör inte det. **(b)** `geometryRows()` misstänktes tappa radgrupperingen vid rotation eftersom vinkelnormaliseringen viker ±90°; ett syntetiskt test av hela kvittot i 21 vinklar (0°–272°) visar att den klarar samtliga realistiska lägen (bara exakt 45° bryts, vilket inget foto ligger på). Ingen kod ändrad för någotdera. De tre verkliga buggarna: **datum** — `receiptDate()` kände igen ISO, europeiskt och svenskt månadsnamnsformat men inte det kompakta `YYMMDD` (`260829`) som svenska kassaregister skriver i huvudet, så datumfältet blev tomt; nu ett sista pass över alla rader (aldrig före ett otvetydigt format) där `validIsoDate` gör urskiljningen, vilket automatiskt utesluter org.nr (`556803` → månad 68) och längre siffersjok. **antal** — formatet `8 Biff Rydberg * 270.00 2160.00`, där `*` markerar styckpriset EFTER namnet i stället för att följa antalet, matchade inte `quantityPattern` och samtliga rader föll tillbaka på antal 1 (8 Biff Rydberg, 7 Vodka & Redbull och 9 Fernet blev alla 1 — förödande just för Snabbnotan, där varje deltagare ska kunna bocka av sin egen öl). Det ledande talet är genuint tvetydigt mot ett menynumrerat namn, så det godtas bara när kvittots egen aritmetik bevisar det: antal × styckpris måste stämma med den tryckta radsumman på öret, annars faller raden igenom till den generiska vägen orörd. **metadata** — `Notanr: 69960E 17.34` blev en köpt rad på 17,34 kr, där 17.34 är det tryckta *klockslaget*; `\bbord\b` matchade inte heller sammansättningen "Bordnr". Nu matchas `(?:nota|bord|kvitto|bong)(?:nr|nummer)` som hela sammansättningar, eftersom lösa `\bnota\b`/`\bbord\b` är vanliga svenska ord som kan inleda ett riktigt rättnamn. Mätt: parser-benchmarken på dev-korpusen (48 fixtures) är identisk före och efter — 100 % på handlare/datum/total/item-F1/pris/antal, exakt avstämning 97,9 %, noll falska metadata-rader — plus fyra nya regressionstester i `tests/receipt-ocr.test.mjs` byggda på det faktiska kvittot. Kvar och medvetet orört: själva textigenkänningen läste samma kvitto dåligt (totalen 5334 → 5934, en rad av tio), vilket varken parsern eller geometrin kan rädda. Kvittot upptar under halva bildrutan och pipelinen beskär aldrig ett vanligt foto till papperet — `locateReceipt()` är trots namnet enbart till för att hitta ett kvitto i en *skärmdump* (mörk UI-list upptill) och returnerar avsiktligt hela rutan annars, och RapidOCR-vägen får ändå råbilden, inte de förbehandlade varianterna. En riktig fotobeskärning före OCR är därför det mest lovande nästa steg, men är en preprocessing-ändring och får enligt reglerna ovan inte skeppas utan en image-mode-benchmark före/efter, vilket kräver den körande OCR-tjänsten.
- v1.29.0:s egen publiceringskörning misslyckades första två gångerna: `docker/build-push-action`s `linux/arm64`-ben (byggt via QEMU-emulering på GitHub:s x86-runners, det enda sättet att korsbygga arm64 utan egen ARM-runner) kraschade med `qemu: uncaught target signal 4 (Illegal instruction)` mitt i `pnpm install --prod --frozen-lockfile`, sannolikt `sharp`s native libvips-bindning som gör CPU-funktionsdetektion QEMU inte emulerar korrekt — ett känt mönster för `sharp` i emulerade multi-platform-bygget, inte orsakat av v1.29.0:s egen diff (`amd64`-benet och hela `test`-jobbet gick igenom rent båda gångerna). Eftersom endast en push till `main` faktiskt kör `publish`-jobbets multi-platform-bygge (PR:ars `test`-jobb bygger bara en enda inhemsk plattform, ingen QEMU inblandad) går felet inte att reproducera eller verifiera en fix mot i en PR — fixen fick pushas direkt mot `main` efter uttryckligt godkännande. Fix: båda `RUN pnpm install`-stegen i `Dockerfile` omslutna med en `until`-återförsöksloop (tre försök, 5 s mellanrum, `exit 1` om alla tre misslyckas) — ingen ändring av det som faktiskt installeras eller av bilden när installationen lyckas första gången, vilket den alltid gjort på riktig x86-hårdvara. Kunde inte byggtestas lokalt (sandboxens nätverkspolicy blockerar `docker.io`-registret), verifierad genom att köra själva återförsöksskal-logiken fristående för alla tre utfall (lyckas direkt, lyckas efter en retry, misslyckas tre gånger) innan push.
- Version 1.29.0 gör det tydligt inne i en resa vilka utgifter den inloggade själv är tillagd i, på uttrycklig begäran ("kan man även se tydligt var man är inlagd på"). Två delar, båda i `TripPage.tsx` och härledda från deltagarens eget `participantId` i just den resan (`trip.participants.find(p => p.userId === user.id)`), ingen ny data och ingen ny synlighet för övriga medlemmar: **(1)** ett filter överst på "Utgifter"-fliken ("Alla utgifter" / "Jag är med (N)") som visar bara de utgifter där `expense.shares` innehåller det egna deltagar-id:t; **(2)** en liten `.mine-badge`-etikett ("Din andel `<belopp>`") bredvid totalbeloppet på varje sådan rad, återanvänd överallt `expenseRow()` redan renderas (Översikt, Utgifter, personvyn) eftersom det är en delad funktion. Iterationen på själva presentationen (badge → inbakad text i metaraden → tillbaka till badge, utan ikonring) var direkt användarstyrd, inte eget tyckande: en fristående synlig etikett visade sig vara lättare att scanna av i en lista än att baka in andelen i den redan gråa "vem betalade"-raden. Ingen ändring av `simplifyDebts()`, splitlogik eller något API-svar. Verifierat: `pnpm typecheck`/`pnpm lint`/`pnpm build:frontend` rena; manuellt i webbläsaren mot en lokal Postgres-databas (eget testkonto, en resa med tre deltagare, en utgift den inloggade är med i och en den inte är med i) i både desktop- och mobilbredd.
- Version 1.28.0 levererar de två punkterna som medvetet sköts upp från 1.26.0:s buggomgång (profilbild och kategoriförslag), på uttrycklig begäran ("Gör resterande nu").
  - **Profilbild** (migration 12): `users.avatar_mime_type`/`avatar_content`, bytes i Postgres precis som kvitton och buggrapportsskärmdumpar — ingen filsystems- eller objektlagring. `POST /api/users/me/avatar` validerar mime-typ/filens faktiska innehåll/bildmått med exakt samma hjälpfunktioner som kvittouppladdning redan använder, normaliserar med en ny `normalizeAvatarImage()` (`sharp`, `fit:"cover", position:"attention"`, 320×320, JPEG) — Sharps "attention"-beskärning hittar den mest detaljrika regionen utan riktig ansiktsigenkänning, ett rimligt startvärde för ett litet, runt avatarformat. `GET /api/users/:id/avatar` kräver bara inloggning, ingen resemedlemskapskontroll, eftersom det här är en liten privat vänkrets. `DELETE /api/users/me/avatar` återgår till initialer. `Avatar.tsx` fick en valfri `userId`-prop: när den finns försöker komponenten ladda fotot och faller tillbaka till initialer vid 404/fel (`onError`), samt en `refreshKey`-prop som tvingar en ny hämtning direkt efter uppladdning/borttagning (annars behåller React samma `<img src>` och webbläsaren skulle aldrig hämta om den). Alla tio ställen i kodbasen som redan renderade `<Avatar>` uppdaterade att skicka med `userId`/`participant.userId` — gästdeltagare utan konto (`userId: null`) visar fortfarande bara initialer, oförändrat. Ändringsytan är avsiktligt bred (tio filer) men varje ändring är en enrads propp-tillägg. Redigeras via en ny rad i "Mer" (kontoidentiteten), med "Lägg till bild"/"Byt bild"/"Ta bort" beroende på om ett foto faktiskt laddades (spårat via en `onPhotoStatus`-callback från `Avatar.tsx`, inte gissat).
  - **Kategoriförslag**: `suggestedCategory()` i `receipt-ocr.ts` (används redan för kvittoskannad text) speglades till en ny `frontend/src/utils/categorySuggestion.ts` — inte ett nytt API-anrop, eftersom reglerna är rena, sällan ändrade nyckelordslistor och ett förslag som uppdateras direkt medan man skriver känns bättre utan nätverksfördröjning. `ExpenseDialog`s titelfält föreslår nu kategori live utifrån vad som skrivs (bara för nya utgifter, aldrig vid redigering av en befintlig), men bara fram tills användaren själv rör kategorivalet — då slutar förslaget skriva över, permanent för den öppna dialogen (`categoryTouched`-flagga). En liten text "Föreslagen utifrån namnet" visas under fältet medan ett förslag aktivt styr valet, så beteendet inte känns som magi.
  - Ingen ändring av `simplifyDebts()`, splitlogiken eller OCR-pipelinen. Verifierat: `pnpm typecheck`/`pnpm lint` rena, hela testsviten grön (75 backend inklusive Postgres-integrationstestet — migrationsräknaren 11→12 uppdaterad igen, samma förväntade engångsjustering som varje ny migration kräver — plus 6 frontend). Manuellt verifierat i webbläsaren mot den lokala utvecklingsdatabasen: en riktig genererad PNG uppladdad via `POST /api/users/me/avatar`, den lagrade bilden hämtad tillbaka och bekräftad som en riktig JPEG (magic bytes `FF D8 FF`, `naturalWidth: 320`), fotot syns korrekt för samma deltagare på ett annat ställe i appen (`balance-row` i en resa), borttagning faller tillbaka till initialer med rätt knapptext, och kategoriförslaget testat både att det träffar rätt ("ICA Kvantum" → Mat och dryck) och att det slutar skriva över efter ett manuellt val.
- Version 1.27.1 rättar en verklig layoutbugg i 1.27.0:s nya "Alla"-huvudkryssruta, hittad av användaren direkt i produktion (skärmbild: kryssrutan låg ovanför texten "Alla", båda centrerade, i stället för sida vid sida). Grundorsak: `.split-select-all` sätter `display: flex` men aldrig `flex-direction` — och elementet är en `<label>`, som en global regel (`label { display: flex; flex-direction: column; ... }`, till för vanliga fält-etiketter där texten ska stå ovanför inputen) redan satt `flex-direction: column` på. CSS:ens kaskad slår ihop reglerna egenskap för egenskap, inte regel för regel: eftersom `.split-select-all` aldrig själv angav `flex-direction` vann den globala `label`-regelns värde ändå, trots lägre specificitet, och kryssruta+text staplades vertikalt i stället för att stå i rad. Fixat med en explicit `flex-direction: row` (plus `justify-content: flex-start`) i `.split-select-all`. Genomsökte samtliga andra `<label className="...">`-användningar i kodbasen efter samma mönster (en egen klass med `display:flex` men utan egen `flex-direction`, med FLERA synliga flex-barn) — inga fler hittades (`.emoji-field`/`.grow` är avsiktligt kolumnorienterade vanliga fältetiketter, och `.receipt-camera-button`/`.button`-etiketterna har bara ett enda synligt flex-barn eftersom deras `<input>` är `hidden`, så riktningen syns aldrig där oavsett). Verifierat: `pnpm typecheck`/`pnpm lint` rena; layouten bekräftad via `getComputedStyle` (`flexDirection: "row"`) mot en färsk CSS-hämtning, eftersom den lokala webbläsarmiljön inte kunde rendera en riktig skärmdump i det här läget.
- Version 1.27.0 är direkt uppföljande UX-feedback på 1.26.0, mottagen medan den byggdes/testades i webbläsaren.
  - **En verklig regression i 1.26.0:s egen fix upptäckt under verifiering, inte antagen.** Google Fonts-länken för Manrope (1.26.0:s fix för buggrapport #5) blockerades tyst av appens egen Content-Security-Policy (`style-src 'self' 'unsafe-inline'`, ingen `font-src` alls — föll tillbaka på `default-src 'self'`). En tidigare webbläsarverifiering hade av misstag läst `document.fonts.check()` som "sant" utan att kontrollera konsolen för CSP-fel; en riktig, fräsch flik med tom konsolhistorik avslöjade "blocked by Content-Security-Policy". Fixat genom att lägga till exakt två externa undantag i CSP:n — `style-src ... https://fonts.googleapis.com` och en ny `font-src 'self' https://fonts.gstatic.com` — inget annat externt ursprung tillåts någonstans i policyn. Verifierat i en fräsch flik: noll konsolfel, `document.fonts` visar faktiskt `status: "loaded"` för Manrope 600/700/800.
  - **"Utgifter" fick en riktig detaljvy i stället för en permissionsspärrad redigeringsdialog.** `ExpenseDialog` stöder nu ett `readOnly`-läge: alla fält blir inaktiverade, "Hantera kategorier" och skicka-knappen döljs (ersätts av "Stäng"), men delningsläge, kategori, betalare och per-persons belopp visas fortfarande tydligt — betydligt mer detaljerat än den tidigare kompakta inline-utfällningen. Hela utgiftsraden (inte en separat "Visa"-knapp) är nu klickbar/tryckbar och öppnar dialogen — skrivskyddad för den som varken är chef eller skapare, redigerbar annars — på uttrycklig begäran ("vill att man bara trycker på utgiften"). Kommentarfunktionen (ny i 1.26.0) flyttades från en egen utfällning i `TripPage` in i `ExpenseDialog` självt, tillgänglig i båda lägena eftersom en kommentar aldrig kräver redigeringsbehörighet — läses nu via `trip.expenses` (uppdateras vid varje `onSaved()`) i stället för den statiska `expense`-propen, så en ny kommentar syns direkt utan att stänga och öppna om dialogen.
  - **"Alla"/"Ingen" ersatt av en enda kryssruta.** De två textknapparna ("Välj alla"/"Avmarkera alla") för deltagarvalet i `ExpenseDialog` kändes onödigt tungt — ersatta med en enda huvudkryssruta ovanför listan (`indeterminate` när ett delurval är gjort, annars helt av/på), samma mönster använt i `PersonDialog` och liknande formulär på webben i allmänhet.
  - **Ny personvy: "gå in på respektive person och vilka utgifter den har lagt till".** Personkort (fliken Personer) och saldoraderna (Översikt → Snabbkoll) är nu klickbara och öppnar en lätt modal som listar alla utgifter där personen är betalare, återanvänder samma `expenseRow()`-radrenderare (klickbar vidare in i respektive utgifts egen detaljvy) — ingen ny backend-endpoint, filtrerar bara `trip.expenses` som redan finns i minnet.
  - **Skrollproblem i nästlade listor inuti dialoger** ("skrollar man 'bakom' menyn"): fyra nästlade skrollbara element inuti dialoger (`.split-people`, `.category-list`, `.contact-results`, `.bug-report-breadcrumbs`) saknade `overscroll-behavior: contain`, så att skrolla till slutet av den inre listan lät skrollningen fortsätta rakt igenom till dialogens egen yttre skroll i stället för att stanna — samma bugg i alla fyra eftersom de delar exakt samma struktur (en `max-height`+`overflow-y:auto`-box nästlad i `.dialog-card`s egen scroll), fixad i alla fyra på en gång.
  - Ingen ändring av `simplifyDebts()`, splitlogiken, migrationer eller autentisering. Verifierat: `pnpm typecheck`/`pnpm lint` rena, hela testsviten grön (75 backend inklusive Postgres-integrationstestet, 6 frontend), manuell webbläsarverifiering av allt ovan mot den lokala utvecklingsdatabasen (kryssrutans indeterminate-tillstånd, kommentar tillagd och synlig utan omöppning, klick genom från personvyn till en utgifts detaljvy, CSP-felfri fräsch sidladdning).
- Version 1.26.0 samlar sex verkliga buggrapporter (Anton Sjösten, Oscar Widelund) och tre uppföljande önskemål, var och en spårad till en konkret grundorsak i den nuvarande koden innan något skrevs.
  - **"Fastnar när man öppnar kvittobild, går ej att kryssa rutan"**: `prepareReceiptFile()` (`imagePrep.ts`) väntade på `canvas.toBlob()` helt utan tidsgräns — till skillnad från själva uppladdningen, som redan var skyddad av `AbortSignal.timeout(30_000)`. `canvas.toBlob` är en dokumenterad hängpunkt på vissa mobila WebViews under minnestryck med stora bilder; om callbacken aldrig anropas hänger `analyze()` för evigt på "Komprimerar kvitto…" och hela dialogen — inklusive deltagarnas kryssrutor i samma formulär — upplevs som fastfrusen. Fixat med en 8-sekunders `withTimeout()`-inpackning som faller tillbaka till originalfilen om komprimeringen hänger, exakt samma "ha alltid en reservväg"-princip appen redan använder för OCR:n.
  - **"Lägg till avmarkera alla när man väljer folk"**: `ExpenseDialog`s deltagarlista hade bara enskilda kryssrutor. Lade till "Välj alla"/"Avmarkera alla" ovanför listan, verifierat i webbläsaren mot alla fyra delningslägen.
  - **"Att det ändras vem som har betalt till mig"** visade sig efter förtydligande handla om något annat än debt-simplification-algoritmen jag först undersökte: "Betalat av" i `ExpenseDialog` föll tillbaka på `trip.participants[0]`, dvs. resans först tillagda deltagare (nästan alltid skaparen) — inte den som faktiskt öppnade dialogen. Fixat genom att slå upp den inloggade användarens egen deltagarpost (`trip.participants.find(p => p.userId === user.id)`) som standardval, med samma `[0]`-fallback kvar för det ovanliga fallet att den inloggade användaren inte är deltagare i resan. `user` skickas nu in som ny prop till `ExpenseDialog` från `App.tsx`.
  - **Vanliga användare kunde inte se en utgifts delningsdetaljer**: `expenseRow()` visade bara totalbeloppet, och den enda vägen till mer detalj var redigeringsknappen — permissionsspärrad till ägare/skapare (`canVoid`). En vanlig deltagare som varken var chef eller skapare hade alltså ingen knapp alls. Lade till en läsbar "Visa"-knapp, oberoende av `canVoid`, som fäller ut per-persons delningsbelopp inline (återanvänder `expense.shares`, ingen ny API-anropspunkt).
  - **Profilbild** och **kategoriförslag baserat på namn** är verkliga, rimliga önskemål men klart större i omfång (schemaändring + ny endpoint respektive en ny förslagsmekanism) — dokumenterade för en separat uppföljning, inte pressade in i den här omgången. (Kategoriförslag har redan en byggsten: `suggestedCategory()` i `receipt-ocr.ts` finns redan för kvittoskannad text och matchar exakt de inbyggda kategorislugsen `food/travel/stay/fun/other` — den körs bara aldrig mot en manuellt inskriven titel idag.)
  - **"Fult typsnitt, inte Times New Roman"**: bekräftat konkret, inte antaget. `frontend/index.html` laddade aldrig något typsnitt — ingen `<link>` mot Google Fonts, ingen `@font-face` — trots att `styles.css` refererar `"Manrope"` på runt 15 ställen. De flesta av dem hade redan `, sans-serif` som reserv och föll tyst tillbaka dit. Men sex regler (`.mini-card strong`, `.avatar`, `.person-card strong`, `.ocr-benchmark-table td`, `.group-row-balance`, `.strip-stat strong` — den sista är den nya mobila resesammanfattningen från 1.24.0, sannolikt vad Anton faktiskt såg) angav `"Manrope"` helt utan reserv i `font:`-kortformen. Utan matchande installerat typsnitt och utan reserv faller webbläsaren tillbaka på sitt eget standardtypsnitt — serif (Times New Roman) på standard-Chrome/Windows. Rättat i två steg: (1) en riktig Google Fonts-länk för Manrope (vikter 600/700/800) så det avsedda varumärkestypsnittet äntligen renderas på riktigt, överallt, för första gången, och (2) `, sans-serif` tillagt på alla tidigare reservlösa Manrope-regler oavsett, som ett andra skyddsnät. Verifierat i webbläsaren: `document.fonts.check('800 16px "Manrope"')` sant efter fixen.
  - Tre ytterligare, uttryckligen begärda tillägg mottagna mitt i arbetet: **kommentarer på en utgift** (helt ny funktion — `expense_comments`-tabell, migration 11, `POST /api/expenses/:id/comments` och `DELETE /api/comments/:id`, synliga för hela resan oavsett redigeringsbehörighet precis som den nya delningsdetaljvyn, borttagning spärrad till chef/skapare via samma `requireRecordWriteAccess()` som kvitton redan använder); **lägg till kvittobild i efterhand på en snabbnota** (fanns redan för resornas utgifter men saknades helt för snabbnotor — servern hade redan en generisk, återanvändbar `POST /api/quick-tabs/:id/receipt` sedan tidigare, så det behövdes bara en ägarknapp i `QuickTabPage`, ingen backend-ändring); **tydligare uppladdningsstatus** ("man tror att den har buggat ur" vid dålig uppkoppling) — lade till "Laddar upp kvittot…"/"Kvittot sparades"-statustext genom hela kedjan (`ExpenseDialog`, `QuickTabDialog`, `TripPage.uploadReceipt`, den nya `QuickTabPage`-uppladdningen) i stället för tyst väntan, samma `receipt-status`-mönster som redan fanns för OCR-skanningen.
  - Ingen ändring av `simplifyDebts()`, splitlogiken, OCR-pipelinen eller autentisering. Verifierat: `pnpm typecheck`/`pnpm lint` rena, hela testsviten grön (75 backend-tester inklusive det stora Postgres-integrationstestet — som behövde en förväntad engångsjustering, migrationsräknaren 10→11 — plus 6 frontend), och manuell webbläsarverifiering mot den lokala utvecklingsdatabasen: kommentar tillagd/borttagen end-to-end via riktiga HTTP-anrop, Välj alla/Avmarkera alla, delningsdetaljvyn, Manrope-laddning och den nya snabbnote-kvittoknappen.
- Version 1.25.1 följer upp den första riktiga RapidOCR-körningen på Unraid (48 offentliga utvecklingskvitton: 100 % köpman/datum/total, 99,5 % artikel-F1, 89,6 % rå avstämning, median 508 ms/P95 1 524 ms). OCR-evidensen visade två generella teckenfel: ett kort svenskt produktnamn med slutligt `l` lästes som `ö1`/`Ö1` på tre kvitton, och första ettan i ett sammanfogat tresiffrigt pris lästes som sista bokstaven i produktnamnet (`...burgarel27,86`). Parsern skapar nu alternativa tolkningar för just dessa teckenformer men accepterar dem endast när heltalsaritmetik i öre får artikelraderna att stämma exakt mot totalen (inklusive kända rabatter); ingen produktlista eller facitfilnamn används. Samma lokala RapidOCR-tjänst och samtliga 48 bilder gick därefter från artikel-F1/pris/antal 99,5/98,5/98,9 % till 100/100/100 %, rå avstämning 89,6 %→97,9 % och rabattjusterad avstämning 91,7 %→100 %, utan falska metadata- eller extrarader. Adminpanelen visar nu både rå och rabattjusterad avstämning och räknar inte ett korrekt rabattkvitto som OCR-avvikelse. Körningarnas latenser är inte jämförbara eftersom den andra lokala Windows-körningen blev termiskt/CPU-mässigt strypt; parserändringen startar ingen extra inferens och har ingen ny modell- eller driftkostnad.
- Version 1.25.0 ersätter Quick Scans GPU-/VLM-beroende produktionsväg med en helt lokal, persistent CPU-tjänst: RapidOCR 3.9.2 och ONNX Runtime med PP-OCRv6-small-detektor samt PP-OCRv5 Latin-mobile-igenkännare. Tjänsten är read-only/non-root, saknar hostport, har checksummerade modeller, bounded queue, tidsgräns, hälsa/readiness och innehållsfria metrics. Node-flödet skickar råa bytes endast till en explicit tillåten intern värd, propagerar avbrott, behåller Tesseract som lokal driftreserv och returnerar ett typat kvitto med null för okända värden, heltalsöre, källevidens och aritmetiska granskningssignaler. Blind normaliseringsretry är av efter mätt regression. Frontend avbryter gamla skanningar och ignorerar sena svar. Den gamla inspekterade "holdout" är omklassad till legacy; ett nytt externt, förseglat schema-v2-facit kördes aggregerat exakt en gång per kandidat. På 48 orörda slutbilder förbättrades artikel-F1 58,5%→91,7%, totalprecision 58,3%→89,6%, artikelhallucinationer 47→2 och medianlatens 1 119→846 ms. Komplett beslut, utvecklingsresultat, holdout, resurser, offline-/integritetsgräns och svagheter finns i `QUICK_SCAN_ARCHITECTURE.md`; maskinläsbara aggregat finns i `tests/ocr-benchmark/results/quick-scan-2026-08-26.json`.
- Version 1.24.3 rättar en verklig, mätt förbättring i hur `combineReceiptPasses()` väljer mellan flera OCR-pass, hittad genom att följa upp användarens fråga om varför "Meny 1"/"Meny 2"-rader ofta saknades på fastfood-kvitton i den riktiga OCR-pipelinen. Undersökningen visade två separata trådar — en avvisad, en skickad.
  - **Avvisad**: att göra bildrätningen (`rectifiedPaperImage`, se `OCR_BENCHMARK.md`s "Rejected experiments") tillgänglig för fler pass regregade hela benchmarken (exakt avstämning 51,2 % → 41,3 %) trots en välgrundad diagnos — mätningen motbevisade hypotesen, så inget skickades.
  - **Skickad, mätt förbättring**: spårning av ett verkligt "Meny 1"/"Meny 2"-fall (fastfood_difficult_dev1 i benchmark-korpusen) ner till Tesseracts faktiska rå-OCR-text visade att `parseReceiptText()` redan hanterar mönstret korrekt (bekräftat mot både facit-text och den riktiga rå-texten från båda Tesseract-passen) — bristen låg i `receiptPassScore()`, som avgör vilket OCR-pass som "vinner" och blir grunden för den slutgiltiga kvittotolkningen. `items.length * 10 000` var obegränsat och vägde 10 gånger mer än en hel 0–100-poängs säkerhetsskillnad, så ett pass kunde vinna enbart genom att hitta FLER rader — inklusive OCR-brus/fragment som inte är riktiga artiklar. Verkligt exempel: Tesseracts renare första pass (5 artiklar, säkerhet 80) förlorade mot sitt eget brusigare andra pass (7 "artiklar", säkerhet 64 — en av dem en osammanhängande textrad från en feltolkad momsrad, en annan ett avbrutet fragment av en annan artikel) enbart på antal rader. Fixat genom att begränsa artikelantalets bidrag till högst 3 artiklars värde (bevarar den redan testade balansen för korta kvitton, där "hittade fler rader" fortfarande är en stark, billig signal) och måttligt höja säkerhetens vikt (`*30`, inte mer — AI-pass bär ett fast metodberoende förtroendevärde här, 70/85/92/94, inte en riktig per-bild-signal som Tesseracts, så en för aggressiv höjning fick ett AI-pass med bara 1 artikel att alltid slå ett Tesseract-pass med 2 artiklar oavsett täckning — fångat av det befintliga testet "local AI receipt output is validated and merged by exact öre"). Nytt regressionstest med det verkliga fastfood-fallet. Verifierat mot hela 80-kvittos benchmarken (bild-läge, både dev och holdout): ingen regression på någon mätpunkt, flera reella förbättringar — exakt avstämning 51,2 % → 55,0 % totalt (dev 50,0 %→54,2 %, holdout 53,1 %→56,3 %), behöver granskning 39/80 → 36/80, artikel-F1 76,7 %→77,6 %, prisnoggrannhet 67,6 %→68,9 %, falska metadata-rader oförändrat 0.
  - Ett andra, separat fynd dokumenterat men inte åtgärdat: sammanslagningslogiken kan fortfarande dra in en omatchad artikel från det förlorande passet om beloppet råkar rymmas aritmetiskt inom vad som återstår till totalsumman, oavsett om namnet ser ut som verkligt innehåll — fångades av ett eget test under utveckling men är inte bekräftat som ett vanligt verkligt problem (benchmarkens `falseMetadataItemsTotal` var 0 både före och efter, oförändrat över hela korpusen), så inte jagat den här omgången.
  - Ingen ändring av `parseReceiptText()`, bildförberedelsen eller någon annan del av pipelinen. Verifierat: `pnpm typecheck`/`pnpm lint` rena, hela testsviten grön (58 backend-tester inklusive det nya + 6 frontend).
- Version 1.24.2 gör det enklare att bjuda in flera personer till en grupp på en gång, på direkt användarfeedback ("Vill även kunna lägga till flera användare samtidigt i en grupp, inte behöva klicka lägg till varje gång... jag vill inte behöva skicka 10 qr koder till 10 vänner"). Undersökning visade att den andra halvan av önskemålet redan fanns i backend: en gruppinbjudan (`invitations`-tabellen) har redan `max_uses` med default 20 och `expires_at` 14 dagar fram — samma länk/QR-kod är redan tänkt att fungera för upp till 20 personer, ingen ny kod krävs per vän. Problemet var att `InviteDialog` aldrig kommunicerade detta: texten talade om "vännen" (singular) och "skanna för att gå med", vilket rimligen fick det att kännas som en engångskod. Ingen backend-ändring behövdes för den delen — bara ärligare text ("Samma länk och QR-kod fungerar för hela gänget... Gäller i 14 dagar och för upp till 20 personer"). Den första halvan var en riktig brist: `PersonDialog` (`Lägg till vän`) stängde sig alltid efter en enda tillagd person — att bjuda in ett helt gäng en och en innebar att öppna om dialogen från grunden för varje person. Dialogen stänger inte längre automatiskt efter en lyckad tillägg; formuläret för gäster nollställs och sökfältet töms så nästa person kan läggas till direkt, en liten räknare ("N personer tillagda hittills") ger löpande bekräftelse, och en uttrycklig "Klar"-knapp stänger dialogen när gänget är fullt. Ingen ändring av `/api/trips/:id/participants` eller `/api/trips/:id/invitations` — ren frontend-ändring i `TripDialogs.tsx`. Verifierat i webbläsaren: två gäster tillagda i samma dialogsession utan att den stängde emellan, räknaren stämde, formuläret rensades korrekt mellan varje, "Klar" stängde och båda personerna fanns kvar i gruppens deltagarlista. `pnpm typecheck`/`pnpm lint` rena, hela testsviten grön (57 backend + 6 frontend) — ingen befintlig test rörde detta flödet i sak.
- Version 1.24.1 rättar en verklig produktionsbugg i 1.24.0:s nya bottennavigering, rapporterad av en riktig användare (buggrapport #3, "Buggad meny") timmar efter att 1.24.0 gått live — bekräftat med en bifogad skärmbild från iPhone (iOS 18.7 Safari) som visade bottennavigeringen liggande mitt i sidinnehållet (mellan Statistik-sidans trendgraf och kategorilistan) i stället för fastspikad längst ner på skärmen. Grundorsak: `.bottom-nav` kombinerade `position: fixed` med `backdrop-filter: blur(12px)` — en känd, väldokumenterad WebKit/iOS Safari-bugg där just den kombinationen kan göra att elementet tyst tappar sin fasta positionering och i stället renderas i det vanliga dokumentflödet. Appens övriga `backdrop-filter`-användningar (`.mobile-header`, `.tabs`) klarar sig eftersom de är `position: sticky`, inte `fixed` — bottennavigeringen var första gången den här appen kombinerat blur med `fixed`. Fixat genom att ta bort `backdrop-filter` och höja bakgrundens opacitet något (`.99` i stället för `.96`) så baren fortfarande läses som en tydlig, solid yta utan den — vilket dessutom ligger närmare designbriefens egen instruktion att undvika en "flytande glaspill"-känsla. Kunde inte reproduceras lokalt (Chromium-baserad testning i den här miljön visade aldrig felet) — diagnosen bygger på det kända buggmönstret och att symptomet i skärmbilden matchar exakt, inte på en egen reproduktion; värt att be den ursprungliga rapportören bekräfta efter nästa driftsättning. Ingen ändring av `.mobile-header`/`.tabs` eller någon annan befintlig `backdrop-filter`-användning. Verifierat: `pnpm typecheck`/`pnpm lint` rena, ren CSS-ändring så ingen testsvit påverkades i sak.
- Version 1.24.0 gör om mobilens informationsarkitektur, på uttrycklig och detaljerad användarbegäran ("inte en kosmetisk 'gör responsiv'-uppgift... nuvarande mobilfrontend beter sig fortfarande som en desktop-dashboard hopträngd till en kolumn"). Fullständig genomgång, mätningar och motivering i `MOBILE_IA_2026.md` — sammanfattning här.
  - **Mätt först**: startsidan hade `scrollHeight` 3202 px vid 390×844 (3,8× skärmhöjden), första gruppkortet syntes inte förrän 821 px ner, rubrik+3 statistikkort tog ensamma de första 446 px, och sidan renderade 13 stora kort samtidigt (alla grupper, alla snabbnotor, alla vänner, hela arkivet — aldrig en förhandsvisning). Gruppvyns översiktsflik hade 3 statistikkort som ensamma tog 477 px innan någon riktig utgifts-/saldoinformation syntes. Mobilnavigeringen var två ikonknappar plus en "⋯"-meny — inget sätt att nå Grupper/Snabbnota/Statistik direkt.
  - **Ny bottennavigering** (Hem · Grupper · Snabbnota · Statistik · Mer), synlig endast vid samma `≤720px`-brytpunkt appen redan använde för att dölja sidopanelen — ingen ny brytpunktsnivå. Aldrig synlig i `guestMode` eller på inloggningsskärmen. Mobilhuvudets gamla ikonknappar och "⋯"-meny togs bort helt (allt de erbjöd nås nu via bottennavigeringen eller en destinations egen primära handling) för att undvika dubblerad navigering i både topp och botten.
  - **Fyra nya toppnivådestinationer**: `GroupsPage` (Aktiva/Arkiv-filter, kompakta rader), `QuickTabsListPage` (Aktiva/Avslutade-filter, "Skanna ny nota" som primär handling), `FriendsPage` (lyft rakt ur startsidans gamla vän-panel) och `MorePage` (kontoidentitet, Vänner/Guide/Buggrapport/Administration [endast admin]/version/logga ut). Ingen ny backend-endpoint eller datahämtning behövdes — alla fyra sidor läser data `refreshDashboard()` redan hämtar. `View`-typen fick fyra nya varianter (`groups`/`quick-tabs`/`friends`/`more`), rent tillägg utan att ändra befintligt hash-format.
  - **Startsidan och gruppvyn beter sig annorlunda på mobil kontra desktop**, avsiktligt via en `useIsMobile()`-hook (`matchMedia("(max-width: 720px)")`, samma brytpunkt som CSS:en) snarare än en ny brytpunkt: desktop renderar exakt samma JSX som innan (verifierat pixel-identiskt, `scrollHeight` 2217 px både före och efter denna ändring vid 1280×900), mobil visar en kraftigt förkortad startsida (hälsning, ett saldokort, tre grupper i förhandsvisning + "Visa alla", fyra senaste utgifterna) och en kompakt gruppöversikt (en sammanfattningsrad i stället för tre stora statistikkort, sekundära handlingar i en "⋯"-meny i stället för fyra knappar i rad).
  - **En verklig bugg hittad under arbetet, inte bara skriven kod**: `.segmented input {position:absolute}` (samma osynliga-radioknapp-över-en-stiliserad-pill-teknik som `ExpenseDialog`s delningslägesväljare redan använde) fungerade bara av misstag — varje befintlig användning råkade ligga inuti en `<dialog>`, vars UA-stilmall ger den `position:absolute` och därmed blev inputens omslutande block. `GroupsPage`/`QuickTabsListPage` är första användningen av `.segmented` utanför en dialog: utan något positionerat föräldraelement blev inputens omslutande block hela viewporten, vilket gav mätbar horisontell överflödning (390 px bred, högerkant utanför skärmen). Fixat vid källan (`.segmented label {position:relative}` plus `.segmented input {inset:0}`), vilket även gör den befintliga dialoganvändningen korrekt av uttrycklig regel snarare än av tur. Hittades genom att mäta `scrollWidth` mot `clientWidth`, inte genom kodgranskning.
  - Ingen ändring av backend, API-kontrakt, finansiell logik eller OCR-pipeline. Verifierat: `pnpm typecheck`/`pnpm lint` rena, hela testsviten grön (57 backend + 6 frontend), det stora Postgres-integrationstestet kört explicit, manuell webbläsarverifiering vid 320/375/390/430/768/1280 px inklusive gästläge (ingen bottennavigering), tangentbordsfokus/Escape, en dialog ovanpå bottennavigeringen, och ett 62 tecken långt gruppnamn (trunkeras korrekt, ingen överflödning).
- Version 1.23.1 rättar tre verkliga buggar i kvittoläsningen, hittade genom att följa upp användarens egen träffsäkra diagnostiska hypotes ("Känns ibland som att när den går vidare till dubbelchecken att det blir riktigt fel... kan det stämma?") och ett konkret exempel användaren sedan delade — ett kvitto från "Skandia Restaurang & Bar", engelskspråkigt med amerikansk sifferformatering ("1,100.00"), som gav ett kraftigt felaktigt resultat i appen efter att dubbelkollen kört.
  - **AI-dubbelkollen skickade fel bild.** `prepareReceiptImages()` förbereder tre separata bildvarianter från samma uppladdning: `ai` (färg-JPEG, normaliserad och skärpt för synmodellen), `grayscale` (avmättad PNG, annorlunda skärpt för Tesseracts egen tolkning) och `binary` (tröskelvärderad för Tesseracts andra pass). Första AI-anropet skickade redan korrekt `images.ai` — men verifieringsomförsöket, som triggas när första AI-passet inte summerar exakt, skickade av misstag `images.grayscale` i stället, buntad som `data:image/jpeg;base64,...` oavsett faktiskt format, så en riktig PNG skickades felmärkt som JPEG. En "andra titt" var alltså i praktiken en sämre, felmärkt bild, inte en genuin självkonsistenskontroll — och eftersom `combineReceiptPasses()`s poängsättning kan låta ett sämre men artikelrikare pass vinna över och skriva över ett i övrigt bättre resultat, kunde dubbelkollen aktivt försämra ett resultat i stället för att bara lägga till brus. Fixat med en enrads ändring (`images.ai` i stället för `images.grayscale`). Nytt regressionstest `tests/receipt-ocr-verification-image.test.mjs`: en lokal mock-PaddleOCR-server fångar de faktiska bilderna som skickas i både första och andra AI-anropet och verifierar att båda är riktiga JPEG (magic bytes `FF D8 FF`), inte en PNG felmärkt som JPEG.
  - **Kommatecken som tusentalsavgränsare lästes fel — den faktiska förklaringen till exemplets trasiga belopp.** `amountCandidates()`/`parseMoney()` kände bara igen mellanslag eller punkt som tusentalsavgränsare (den svenska/tyska konventionen), aldrig kommatecken (den engelska/amerikanska konventionen, "1,100.00"). Ett belopp som "2,200.00" fick sitt inledande "2," tyst kapat och lästes som "200.00", med "2," kvarlämnat i artikelnamnet — reproducerat exakt mot exemplet: "Grilled Tomahawk Steak 2 1,100.00 2,200.00" blev en artikel med namnet "Grilled Tomahawk Steak 2 1, 2" och beloppet 200.00 i stället för 2200.00, och hela totalsumman gick förlorad. `parseMoney()` skrevs om för att hitta det SISTA kommatecknet eller punkten i ett belopp (alltid decimaltecknet, eftersom varje anropsställe redan kräver exakt två avslutande siffror) och tolka alla tidigare kommatecken/punkter som tusentalsavgränsare — verifierat manuellt mot alla fyra konventioner (mellanslag+komma, punkt+komma, komma+punkt, ren decimal) innan `amountCandidates()`s reguljära uttryck (samt två syskonuttryck för samma mönster) utökades att även acceptera komma i tusentalsgruppen. Nytt regressionstest verifierar att exemplets belopp nu blir exakt 2200.00/3100.00/5300.00.
  - **Telefonnummer och serviceavgift blev falska artikelrader.** Butikens telefonnummerrad ("Telephone: +46 8 123 45 67") och en serviceavgift ("SERVICE (10%)") tolkades båda som köpta artiklar — `metadataLineWords` saknade helt enkelt orden för det. Tillagt: `serveringsavgift`/`service` och `telefon`/`tel`/`telephone`/`phone`, samtliga ord-gränsade som resten av listan. Båda är vanligt förekommande på riktiga svenska kvitton också (inte specifikt för exempelkvittot), så detta är en generell förbättring, inte en punktfix.
  - Alla tre fixar verifierade oberoende mot exemplets faktiska text innan sammanslagning: kvittot ger nu korrekta artikelrader (Caviar & Blinis 950.00, Grilled Tomahawk Steak 2200.00, Lobster & Saffron Pasta 480.00, Château Margaux 3100.00, Water/Coffee/Tiramisu korrekta), korrekt totalsumma, och varken telefonnumret eller serviceavgiften som artikel. Kvarvarande brist, medvetet inte åtgärdad: kvittots fyrkolumnslayout (namn/antal/styckpris/radsumma) känns inte igen som sådan, så antalskolumnens siffra hamnar kvar i artikelnamnet i stället för i `quantity`-fältet ("Grilled Tomahawk Steak 2" i stället för ren "Grilled Tomahawk Steak" med `quantity: 2`) — kosmetiskt fel som användaren ser direkt vid granskning, inte en tyst felaktig summa, och specifikt för en icke-svensk kvittolayout som ligger utanför produktmålet ("läsa svenska kvitton"). Ingen ändring av när dubbelkollen triggas, poängsättningen i `combineReceiptPasses()`, eller `parseAmount()` (formulärfältens separata gräns, orörd). Verifierat: hela testsviten grön (57 backend-tester inklusive de tre nya, plus 6 frontend), `pnpm typecheck`/`pnpm lint` rena, samt en separat körning av det stora Postgres-integrationstestet lokalt (det nya OCR-benchmark-adminrutttestet från 1.23.0, som bara körs när `TEST_DATABASE_URL` är satt, exekverades faktiskt denna gång i stället för att bara typkontrolleras).
- Version 1.23.0 flyttar OCR-benchmarken (`tests/ocr-benchmark/`, se `OCR_BENCHMARK.md`) in i appen som en admin-panel, på direkt användarbegäran efter att SSH/Docker-vägen till Unraid-servern visade sig ha för mycket friktion (`pnpm` saknades på värden, checkouten i `/mnt/user/kompis_split/app` innehöll bara `compose.yaml`, inte källkoden). En admin ser nu ett "OCR-benchmark"-kort i Administration → Kvalitetskontroll med två knappar: "Snabb kontroll" (parser-läge, deterministiskt, inget OCR/GPU, klart på millisekunder) och "Verklig OCR-pipeline" (anropar samma `recognizeReceipt()` som riktiga kvittouppladdningar — samma lokala Tesseract alltid, plus PaddleOCR-VL när `PADDLEOCR_URL` faktiskt är nåbar, exakt den miljövariabel produktionen redan är konfigurerad med). Rapporten visar köpman/datum/total/artikel-F1/pris/antal-träffsäkerhet, exakt avstämning, antal falska metadata-rader, hastighet och — viktigast för att aldrig vilseleda om vad som faktiskt testades — vilken källa varje kvitto löstes med (`tesseract` kontra `paddleocr+tesseract`), plus en lista över vilka specifika testkvitton som avvek.
  - **Arkitektur**: den centrala matchnings-/poängsättningslogiken (tidigare bara i `tests/ocr-benchmark/scoring.mjs`) flyttades till en ny `src/ocr-benchmark.ts`, kompilerad till `dist/ocr-benchmark.js` precis som resten av backend — CLI-verktyget importerar nu därifrån i stället för att ha en egen parallell kopia, så de två aldrig kan råka ge olika svar. Samma modul laddar korpusen (`tests/ocr-benchmark/corpus/`, samma relativa sökväg fungerar oförändrat lokalt och i Docker-imagen) och kör antingen `parseReceiptText()` direkt (parser-läge) eller den riktiga `recognizeReceipt()` (bildläge).
  - **Bakgrundsjobb, inte en blockerande förfrågan**: en full körning av bildläget mot 80 kvitton kan ta någon minut (varje kvitto kan trigga ett riktigt PaddleOCR-VL-anrop), vilket riskerar att en vanlig HTTP-förfrågan kapas av Cloudflare/Nginx Proxy Managers timeout-gränser i onödan. `POST /api/admin/ocr-benchmark` startar därför jobbet i bakgrunden och svarar direkt (202); `GET /api/admin/ocr-benchmark` returnerar aktuellt jobbs status/progress/resultat, som adminpanelen pollar var 2:a sekund medan jobbet kör. Jobbet hålls bara i minnet (försvinner vid omstart, precis som annan diagnostikdata) och är globalt (bara ett jobb i taget, oavsett vilken admin som startade det) eftersom det är en delad diagnostikresurs, inte per-användardata.
  - **Behörighet**: `requireAdmin` precis som övriga admin-only-rutter, och tillagd i den centrala demolägesnekningslistan i `handleApi()` (`/^\/api\/admin\/ocr-benchmark(\/.*)?$/`) — samma princip som redan gäller för e-postinställningar och buggrapporter: inga globala adminfunktioner är tillgängliga från en demosession, även om just den här funktionen i sig inte rör resedata.
  - **Docker**: `Dockerfile` kopierar nu `tests/ocr-benchmark/corpus` (bilder + facit, cirka 3,4 MB, inga byggsteg behövs) till produktionsimagen, oförändrat läsbart i den read-only-monterade containern eftersom det bara någonsin läses, aldrig skrivs.
  - Verifierat i webbläsaren mot den lokala utvecklingsdatabasen: "Snabb kontroll" gav exakt samma siffror (100 % köpman/datum/total/artikel-F1/pris/antal) som CLI-verktyget oberoende visat; "Verklig OCR-pipeline" körde alla 80 kvitton via bakgrundsjobbet med korrekt live-progress ("N/80 kvitton bearbetade…"), avslutade med `tesseract: 80` korrekt synligt (ingen PaddleOCR-VL konfigurerad lokalt) och siffror som matchade den tidigare fristående CLI-körningen. Nya tester: `tests/ocr-benchmark.test.mjs` (3 st, deterministiska, kör modulens egen sökvägsupplösning och parser-benchmark) samt utökade behörighets-/funktionsassertions i den stora integrationstesten (icke-admin nekas, admin kan starta och läsa ett färdigt jobb med rimliga siffror).
- Version 1.22.1 rättar en verklig, användarrapporterad bugg (buggrapport #2): vid uppgörelse av flera skulder i samma grupp efter varandra ("gör upp"-fliken) sparades fel belopp för alla betalningar utom den första, trots att Swish-länken alltid visade rätt belopp. Grundorsak: `App.tsx` monterar `PaymentDialog` en enda gång för hela resesessionen (`{trip && user && <>...<PaymentDialog .../></>}`), och beloppsfältet använde `defaultValue` — som i React bara sätts vid själva DOM-nodens första montering, aldrig om senare. Andra, tredje osv. gången någon klickade "Markera betald" för en *annan* skuld återanvändes samma `<input>`-nod och beloppet förblev tyst kvar på det första beloppet, även om dialogens rubrik ("X → Y") korrekt uppdaterades. Bekräftat i webbläsaren: skuld 1 (175,00 kr) sparades rätt, skuld 2 (100,00 kr, en annan person) visade och skulle ha sparat 175,00 kr utan fixen. Fixat med samma `key`-baserade återställningsmönster som `FriendInviteDialog` redan använde ett par rader ovanför — `key={fromId}-{toId}-{amountCents}` tvingar React att montera om hela dialogen (formulär + felmeddelande) varje gång skulden faktiskt ändras. Hela `frontend/src`s `defaultValue`-användning genomsöktes efter sammanslagningen för att bekräfta att inget annat ställe har samma sårbarhet (`ExpenseDialog`s redigeringsläge är redan korrekt: kontrollerade fält plus en explicit återställningseffekt vid öppning) — det fanns bara ett till, ett statiskt standardvärde på kategorins emoji-fält, opåverkat av några ändrade props. Verifierat i webbläsaren mot en riktig flerpersonsskuld (Anna 175 kr, Erik 100 kr efter olika utgifter): registrera Annas betalning, öppna sedan Eriks — beloppsfältet visar nu korrekt 100,00 kr i stället för att fastna på 175,00 kr.
- Version 1.22.0 är en mätdriven OCR-noggrannhetsgenomgång av kvittoläsningen, på användarens uttryckliga begäran ("measure → reproduce → experiment → benchmark → improve → validate", inte bara fler regexar). Kärnleveransen är en ny, repeterbar benchmark (`tests/ocr-benchmark/`, `pnpm benchmark:ocr`/`pnpm benchmark:ocr:corpus`) mot en 80-kvittos syntetisk svensk korpus (8 verksamhetstyper × 4 svårighetsnivåer, 48 dev/32 holdout, deterministiskt genererad från SVG-mallar via Sharp — se `tests/ocr-benchmark/SOURCES.md` för varför inga riktiga/publika kvittobilder användes) med exakt maskinläsbar facit i öre. Fullständigt experimentlogg, felkategorisering och siffror i `OCR_BENCHMARK.md`.
  - **Mätt först, sedan åtgärdat**: parser-only-läget (matar facitens "perfekta OCR-text" rakt in i `parseReceiptText()`, inget GPU/OCR behövs) gick från en uppmätt baslinje (köpman 51,2 %, datum 35,0 %, artikel-F1 96,8 %, exakt avstämning 52,5 %) till 100 % på köpman/datum/total/artikelprecision/artikelträff/artikel-F1/pris/antal, samt 96,3 % exakt avstämning (100 % när kända rabatter räknas med — de återstående 3/80 är kvitton med en riktig rabattrad, som appen redan avsiktligt flaggar för granskning snarare än att låtsas stämma).
  - **10 verkliga, verifierade buggar hittade och fixade** i `src/receipt-ocr.ts`, samtliga generella strukturella fixar (aldrig en regel skriven kring ett enskilt facit-kvitto): (1) ett datum som "11 jul 2025" eller "19/06/2025" fick sitt årtal feltolkat som ett pris av samma heuristik som reparerar ett OCR-kvitto utan decimaltecken (t.ex. "13000" → "130,00") — högsta enskilda träffen, rättade även merparten av avstämningsfelen på köpet eftersom det korrupta datumet blivit en påhittad artikelrad; (2) `excludedTotalWords`s ord-gränslösa "vat" matchade "Mineralvatten" som substräng och uteslöt den som artikel; (3) ett menynumrerat namn ("Sushi meny 1 159,90") kolliderade med den (redan testade, nödvändiga) tolkningen av mellanslag som tusentalsavgränsare i svenska belopp ("1 234,50"); (4) köpmansigenkänningen dominerades av ren bokstavsräkning och förlorade konsekvent mot adressraden för korta/vanliga företagsnamn — nu viktad med radposition, gatunamnssuffix- och kommastraff samt ett tak på bokstavsbidraget; (5) samma ord-gränsproblem som (2) i `businessWord` ("butik" matchade inuti "Butiksgatan"); (6) antalsformatet "Öl 2 st 158,00" (antal efter namnet) kändes inte igen alls; (7) fyra separata namnstädningsregex byttes till en delad `\p{L}`/`\p{N}`-baserad variant eftersom den smalare `[A-Za-zÅÄÖåäö]`-klassen tyst klippte bort andra latinska diakritiska tecken svensk text ändå lånar ("Frukostbuffé" → "Frukostbuff"); (8) den fristående enhetsprismarkören "à" ("2 x Vin à 95,00 190,00") städades aldrig bort eftersom `\b` inte pålitligt avgränsar en icke-ASCII-bokstav utan `u`-flaggan.
  - **En självupptäckt regression**: fix (4)s gatunamnssuffix fick av misstag en OCR-tolerant "gränd"→"grand"-variant, vilket gjorde att det vanliga, legitima ordet "Grand" (som i "Grand Hotellets Matsal") plötsligt poängsattes som en adress. Fångat av benchmarken innan sammanslagning, inte av mänsklig korrekturläsning — precis vad verktyget är byggt för.
  - **Riktig bildpipeline (verklig Tesseract-OCR, CPU, ingen GPU tillgänglig i den här miljön)**: köpman gick från 77,1 % till 83,3 % på dev-uppsättningen (samma fix som (4) ovan, nu mot verkligt OCR-brus). Fullständig korpus (80 kvitton): köpman 82,5 %, datum 73,8 %, total 81,3 %, artikel-F1 76,7 %, pris 67,6 %, antal 73,1 %, meduntid 871 ms/P95 1429 ms. Detta mäter enbart Tesseract-reservvägen (den kodväg appen redan har en lösning för — `recognizeReceipt()`s befintliga adaptiva eskalering till PaddleOCR-VL när Tesseract inte själv stämmer av), inte den fulla produktionspipelinen; exakt kommando för att köra samma benchmark mot PaddleOCR-VL på GTX 1080 Ti finns i `OCR_BENCHMARK.md`.
  - **Medvetet inte åtgärdat**: en sällsynt sammansatt bugg där en enda OCR-felläsning (t.ex. "33cl" mistolkat som "3301") kan bryta ihopparningen mellan en "alla namn, sedan alla priser"-layout och därmed tappa hela blocket — en mer toleranrant ihopparningslogik skulle riskera att av misstag suga in helt vanliga kompletta artikelrader i samma insamling. Dokumenterad i `OCR_BENCHMARK.md` som en känd svaghet, inte gissad bort.
  - Ingen ändring av autentisering, behörigheter, finansiell kärnlogik (`allocateByWeights`/`simplifyDebts` orörda — bara hur ett skrivet värde tolkas till ett tal ändrades, samma gräns-princip som `parseAmount()`/`calculateShares()` redan följer sedan 1.21.0), kvittouppladdningssäkerhet, demoläge eller Docker-isolering. Verifierat: 39 backend-tester (inklusive 11 nya regressionstester, en per fix ovan) + 6 frontend-tester, `pnpm typecheck`/`pnpm lint` rena, hela benchmark-korpusen i parser-läge (0 fel) och i verkligt bildläge (dokumenterat ovan).
- Version 1.21.0 är en produktbred UX-/mobilanvändbarhets-/visuell-designgenomgång, på användarens uttryckliga begäran (en detaljerad brief om navigationsarkitektur, mobilanvändbarhet och visuell polering). Ingen arkitektur, finansiell logik, OCR-pipeline eller demolägesisolering ändrades — se `UX_AUDIT.md` för fullständiga fynd, motiveringar och permanenta designprinciper. Två P0-fel (trasiga/missvisande), fem P1 (stor användbarhetspåverkan) och fyra P2 (viktig tydlighet/konsekvens) åtgärdades; lågvärdig kosmetisk P3-polering lämnades uttryckligen orörd.
  - **P0: Beloppsfält avvisade tyst det svenska decimalkommat.** `type="number"`-fält töms av webbläsaren så fort ett komma skrivs (bekräftat direkt: `input.value = '249,50'` blir `''`), oavsett `lang`/`inputMode`. Fixat i hela kedjan, inte bara i fältet: fem `<input>` i `ExpenseDialog.tsx`/`QuickTabDialog.tsx`/`TripDialogs.tsx` bytta till `type="text" inputMode="decimal"` med tecken-filter (samma mönster som antalfältet redan använde sedan 1.16.0), plus en ny `parseDecimal()`-hjälpfunktion i `src/split.ts` och en uppdaterad `parseAmount()` i `src/server.ts` så att servern — den faktiska gränsen där en klient inte kan lita på frontends inputtyp — accepterar `249,50` och `249.50` identiskt. Nya tester i `tests/split.test.mjs` och en fullständig skapa/verifiera/makulera-regression i `tests/server.integration.test.mjs`.
  - **P0: Mobilhuvudet flödade över horisontellt.** Uppmätt, inte antaget: vid 375 px bredd tvingade sex ikonknappar plus versionsbadge `document.documentElement.scrollWidth` förbi `clientWidth`. `Shell.tsx`s mobilhuvud byggdes om till varumärke + två primära ikonknappar (Ny grupp, Snabbnota) + en enda 44×44 "⋯"-knapp som öppnar en liten meny (Statistik/Användarguide/Administration/Rapportera en bugg), med stängning vid klick utanför, `Escape` eller val av ett menyalternativ. Omätt vid 320/375/390/430 px efter fixen: ingen horisontell överflödning vid någon bredd (en kvarvarande 2 px-överflödning vid exakt 320 px spårades till versionsbadgen och löstes genom att dölja den under 360 px).
  - **P1 × 5:** påminnelseknappen på startsidan visas nu bara när `net > 0` och sitter i saldokortet i stället för handlingsraden (`DashboardPage.tsx`); gruppsidans och startsidans mobila handlingsrader har primär handling först i DOM-ordning i stället för sist, eftersom mobilens rutnätslayout följer markup-ordning (`TripPage.tsx`, `DashboardPage.tsx`); sidopanelens resmeny grupperar Statistik/Guide/Admin/Buggrapport i en egen sektion ovanför kontofoten och listan över grupper flyttas upp direkt under de primära skapa-knapparna (`Shell.tsx`); `viewport-fit=cover` saknades i viewport-metataggen, vilket tyst gjorde alla befintliga `env(safe-area-inset-*)`-regler verkningslösa på iPhone med hack — tillagt, plus motsvarande `env(safe-area-inset-top)` på mobilhuvudet; ikonknappar var 38×38 px, höjda till 44×44 px.
  - **P2 × 4:** `initials()` i `format.ts` tog bokstavligen första tecknet i varje ord, så ett gästnamn som `-Anna Svensson` gav `-S` i stället för `AS` — hittar nu första faktiska Unicode-bokstaven per ord (nya tester i `format.test.ts`); Snabbnotas skapa-ikon ("✓", som liknade en avbockning) byttes till "⚡" i sidopanel och mobilhuvud; ingen synlig tangentbordsfokusring fanns på ikonknappar, resmenylänkar, textknappar, kvittolänkar, det segmenterade delningsvalet eller kryssrutor för deltagare — `:focus-visible` utökad överallt, inklusive samma teknik för det dolda `<input>`-baserade segmenterade valet (ringen läggs på det synliga syskonelementet); dialoger saknade `aria-labelledby` — `Modal.tsx` taggar nu `DialogHeader`s `<h2>` med ett `useId()`-genererat id och kopplar det till dialogens `aria-labelledby` automatiskt, utan att röra ett enda anropsställe.
  - `--muted` (#6d716b på #f5f0e8) beräknades till ≈4,38:1 kontrast, precis under WCAG AA:s 4,5:1-gräns för normal text — mörkat till `#63675f` (≈5,1:1).
  - Varumärkesidentitet (den varma papper/korall/koboltblå/mint/sol-paletten), Hammarby- och öl-easter eggs, demoläge, OCR-pipeline och den faktiska delnings-/avräkningsmatematiken lämnades avsiktligt orörda — granskade men inte ändrade. Ingen ramverksmigrering, inget nytt ikonbibliotek, ingen `App.tsx`-omskrivning.
  - Verifierat: `pnpm typecheck`/`pnpm lint` rena; `pnpm test` (39 `node --test`, inklusive hela konto-/inbjudnings-/behörighets-/arkiv-/audit-integrationstestet mot en riktig Postgres-databas via `TEST_DATABASE_URL`, körd explicit snarare än lämnad att auto-hoppas över) + 6/6 vitest, alla gröna; manuell webbläsarverifiering av inloggning, startsida, mobilhuvud vid 320/375/390/430 px (uppmätt nollöverflödning), menyns öppna/stäng-beteenden, sidopanelens DOM-ordning, gruppsidans handlingsordning och dialogers `aria-labelledby`-upplösning.
- Version 1.20.3 rättar en verklig race condition, hittad av en bakgrundsutredning av tre återkommande, till synes slumpmässiga CI-testfel (misstänkt CI-infrastrukturbrus i utgångsläget, men grävdes vidare i på begäran). `POST /api/trips`-hanteraren var det enda transaktionella stället i `src/server.ts` som anropade `json(response, ...)` (som avslutar HTTP-svaret direkt) **inifrån** `db.transaction()`s arbetsfunktion, innan `COMMIT` faktiskt körts efteråt. Svaret 201 kunde alltså nå klienten innan den nya gruppens rader var synliga för andra anslutningar — på CI:s Postgres-tjänstcontainer, där commit-omgången är långsammare relativt loopback-HTTP, var fönstret brett nog för nästa begäran (på en annan pool-anslutning) att missa den nyss skapade gruppen. Det förklarade båda observerade CI-felen (adminöversikten en grupp för kort, inbjudan på en nyskapad grupp fick 403 eftersom `trip_access` inte var synlig än). Fixat genom att returnera `tripId` från transaktionen och anropa `json()` först efter att den har körts klart — samma mönster som alla andra transaktionella rutter redan följde. Nytt regressionstest frågar `trip_access` på en helt separat databasanslutning direkt efter att gruppen skapats, vilket reproducerar racet exakt om det återkommer. Verifierat: full testsvit + 4 rena lokala körningar i rad utan flakighet.
- Version 1.20.2: tre separata saker, på användarens begäran.
  - **PWA/hemskärmsinstallation**: appen kan nu installeras som en app-liknande ikon på mobilens hemskärm utan App Store/Play Store, på frågan "Finns det något sätt att enkelt göra det så att de får den som en app liknande på mobilen". Ny `public/manifest.json` (namn, ikoner, `display: standalone`, tema-/bakgrundsfärg matchande appens `--paper`), tre genererade PNG-ikoner (`icon-192.png`, `icon-512.png`, `apple-touch-icon.png`, coral bakgrund med "KS" i samma stil som `.brand-mark`) samt `<link rel="manifest">`, `apple-touch-icon` och `apple-mobile-web-app-*`-metataggar i `frontend/index.html`. `frontend/vite.config.ts`s befintliga statiska kopieringssteg (som redan kopierade `styles.css` till `dist/` eftersom `publicDir` är avstängt) utökat till att kopiera alla dessa filer. Ingen service worker — bara installerbarheten efterfrågades, inte offline-stöd.
  - **Bugfix**: en riktig buggrapport från användaren (skärmbild bifogad) visade `null is not an object (evaluating 'e.currentTarget.reset')` vid skapande av ny grupp efter att ha navigerat tillbaka på mobilen. Grundorsak: `event.currentTarget` lästes efter ett `await` i submit-handlern — `React.FormEvent`s `currentTarget` är bara garanterat giltig synkront under själva händelsen, och kan bli `null` om sidan omflödas mitt i (t.ex. vid bakåtnavigering på iOS). Samma mönster fanns på ytterligare ett ställe (kategori-skapande). Båda fixade genom att fånga `const form = event.currentTarget` synkront innan `await`, samma `form`-referens använd genomgående. De två andra ställen i kodbasen som redan läste `event.currentTarget` gjorde det korrekt (synkront, före ev. `await`) och lämnades orörda.
  - **Exportera buggrapport**: ny "📋 Kopiera för utvecklare"-knapp i adminpanelens buggrapport-popup, på begäran ("Vill ha ett bra sätt att exportera bugg-rapporter och skicka till dig utan att behöva ta skärmbilder"). Kopierar en ren textversion (rapport-ID, status, rapportör, beskrivning, teknisk information, länk till ev. skärmbild, senaste API-anrop) till urklipp med `copyText`, redo att klistras in direkt i en chatt — ingen skärmbild behövs längre. `BugReport` bröts ut till en egen typ i `frontend/src/types/models.ts` för återanvändning.
- Version 1.20.1 gör 1.20.0:s betalningspåminnelser till en vanlig funktion för alla konton, inte en adminfunktion, på direkt användarfeedback efter att 1.20.0 gått live ("Påminnelsen ska vara tillgänglig för alla inte bara admins, ska vara påmin de som är skyldig dig"). `POST /api/admin/remind-unpaid` (adminbegränsad, påminde ALLA i hela appen med obetalt) ersatt av `POST /api/remind-unpaid` (vilket inloggat konto som helst, alltid strikt avgränsat till skulder mot den som anropar — `unpaidRemindersFor(creditorUserId)` filtrerar resors avräkningar på `creditor.user_id === creditorUserId` och snabbnotor på `created_by = creditorUserId`, aldrig andras skulder). Knappen flyttad från adminpanelens E-post-sektion till en ny "🔔 Påminn om obetalt"-knapp på startsidan, bredvid Statistik/Ny grupp — tillgänglig för alla inloggade användare. E-postens ämnesrad och brödtext namnger nu vem som skickar påminnelsen (`Påminnelse från {namn}`) eftersom mailen går från en delad avsändaradress, inte en personlig. Adminpanelens E-postinställningar (Graph-uppgifter, testmail) är oförändrat adminbegränsade — det är bara själva påminnelseutskicket som öppnats upp. Demoläges-denylistan uppdaterad till den nya rutten. Verifierat: en gäldenär som anropar rutten får `sent: 0` och skickar inget mail; fordringsägaren får rätt mottagare och belopp.
- Version 1.20.0 bygger den faktiska e-postintegrationen ovanpå 1.19.2/1.19.3:s uppsättningsskript, på användarens begäran ("under admin en email test och settings för det", "reset password function", "remind users of their unpaid expenses"). Tre delar:
  - **E-postinställningar under Administration**: ny sektion "E-post" i adminpanelen — tenant-ID, klient-ID, klienthemlighet och avsändaradress sparas i en ny singleton-tabell `email_settings` (migration 10), inte miljövariabler, så en admin kan konfigurera det direkt i appen efter att ha kört uppsättningsskripten. `GET /api/admin/email-settings` returnerar aldrig klienthemligheten (bara `hasSecret: boolean`); att spara utan att fylla i hemligheten behåller den gamla i stället för att nolla den. `src/email.ts` sköter app-only OAuth2 mot Microsoft Graph (client-credentials, tokencache, `POST /v1.0/users/{avsändare}/sendMail`) — samma flöde `scripts/test-email-send.ps1` redan verifierat manuellt. En "Skicka testmail"-knapp (`POST /api/admin/email-settings/test`, hastighetsbegränsad) skickar ett riktigt testmail och visar Graph-felet rakt av om något är fel konfigurerat.
  - **Lösenordsåterställning**: "Glömt lösenord?" på inloggningssidan → `POST /api/auth/forgot-password` (hastighetsbegränsad per IP) → alltid samma generiska svar oavsett om adressen finns, för att inte läcka vilka e-postadresser har konton. Token hashas (`password_reset_tokens`, migration 10), gäller 30 minuter, är engångsgiltig. Länken (`#reset-password=…`) öppnar ett nytt AuthScreen-läge; en lyckad återställning byter lösenordshash och loggar ut kontot från alla enheter (`DELETE FROM sessions`), precis som att inaktivera ett konto redan gjorde.
  - **Betalningspåminnelser**: "Skicka påminnelser" i adminpanelens E-post-sektion (`POST /api/admin/remind-unpaid`) skickar ett samlat mail per registrerad användare med alla obetalda belopp — beräknat från resors avräknade skulder (`simplifyDebts`-uträkningen, samma som `loadTrip` redan använder) och obetalda snabbnotaandelar (`loadQuickTab`s `personTotals`, filtrerat på `paidAt IS NULL`). Endast till registrerade användare med konto, aldrig snabbnotagäster (saknar e-post). Admin ser hur många som skickades och eventuella enskilda fel.
  - Verifierat med en ny, riktig testrigg: integrationstesterna spinner nu upp en liten lokal mock-server som ersätter de riktiga Microsoft Graph-endpointerna (`src/email.ts` läser `GRAPH_TOKEN_BASE`/`GRAPH_API_BASE`, satta till mocken bara i testmiljön — obestämda i produktion, där de riktiga Microsoft-värdarna används som tidigare) — så hela kedjan (spara inställningar → skicka testmail → skicka påminnelser → glömt lösenord → öppna den riktiga länken → återställ → gammal session ogiltig, token engångsgiltig) testas på riktigt, inte bara mockat bort. Verifierat även manuellt i webbläsaren (adminpanelens formulär, testmail-fel vid fejkade uppgifter, påminnelseknappen, glömt lösenord-länken, ogiltig-token-felet).
- Version 1.19.2 lägger till två fristående PowerShell-skript i `scripts/`, på användarens begäran, inför framtida lösenordsåterställning via e-post (SMTP AUTH fasas ut från Exchange Online). Ingen backend-/frontend-kod, ingen databasändring, ingen ny appfunktion — skripten körs av användaren själv mot deras eget Microsoft 365-tenant, aldrig av mig. `scripts/setup-email-integration.ps1` använder Microsoft.Graph PowerShell-SDK:t direkt (inte Azure CLI — kräver ingen Azure-prenumeration, bara Entra ID som redan ingår i alla M365-tenanter, på användarens uttryckliga begäran) för att skapa/återanvända en app-registrering ("Kompis Split Mail"), bevilja Graph-behörigheten `Mail.Send` (application, GUID `b633e1c5-b582-4048-a93e-9f11b44c7e96`) genom att direkt skapa app-role-tilldelningen (det är själva admin-consent-steget), skapa en klienthemlighet och skriva ut färdiga miljövariabler (`GRAPH_TENANT_ID`/`GRAPH_CLIENT_ID`/`GRAPH_CLIENT_SECRET`/`GRAPH_SENDER_EMAIL`) att klistra in i Compose-miljön. Körs säkert flera gånger (återanvänder befintlig app-registrering, skapar aldrig en ny klienthemlighet utan uttrycklig bekräftelse). `scripts/scope-email-sender.ps1` är ett rekommenderat andra steg: skapar en Exchange Online Application Access Policy (`New-ApplicationAccessPolicy`) så appen bara kan skicka som en vald avsändarmailbox i stället för valfri brevlåda i tenanten, vilket begränsar skadan om klienthemligheten någonsin läcker. Policyn skopas via en mailaktiverad säkerhetsgrupp (`kompis-split-senders@<domän>`) som skriptet skapar/återanvänder och lägger avsändarmailboxen i, i stället för att peka `-PolicyScopeGroupId` direkt på mailboxen — upptäckt genom verklig felsökning mot användarens tenant: att peka direkt på en mailbox (även en fullt synkad delad brevlåda med giltigt `ExternalDirectoryObjectId`) gav konsekvent felet "the identity of the policy scope is not a security principal", vilket matchar Microsofts egen dokumenterade rekommendation att alltid skopa via en grupp. Faktisk backend-integration mot Microsoft Graph (`sendMail`) och lösenordsåterställningsflödet är inte byggt än — det är nästa steg när användaren har kört skripten.
- Version 1.19.1 är en liten polerings-/kul-omgång, på användarens begäran. Statistik-ikonen (⌁, tvetydig) byttes till 📊 överallt den används; alla ikon-only-knappar i mobilhuvudet fick konsekventa `title`-attribut. Två rent kosmetiska easter eggs, aldrig kopplade till splitlogiken: (1) klicka logotypen fem gånger snabbt (inom 2,5 s) visar ett grönvitt Hammarby-tema-meddelande i 3,2 sekunder; (2) kvittorader i Snabbnota vars namn matchar öl-relaterade ord (öl, pilsner, lager, ipa, stout, porter, ale, beer) får en 🍺 bredvid namnet. Inget nytt API, ingen databasändring.
- Version 1.19.0 lägger till en inbyggd användarguide, på användarens begäran. Ny sida (`#guide`, länk "？ Användarguide" i sidomenyn och mobilhuvudet) med rena statiska förklaringstexter — inget nytt API, ingen databasändring. Täcker: när man ska använda Grupp kontra Snabbnota, steg-för-steg för båda flödena (inklusive den nya "Markera betald"-funktionen), tips för kvittofoto/OCR, vad statistiksidan visar, och hur man rapporterar en bugg. En extra sektion med adminfunktioner visas bara för administratörer.
- Version 1.18.0 lägger till inbyggd buggrapportering, på användarens begäran. Klicka "Rapportera en bugg" (sidopanelen, eller ikon i mobilhuvudet) → skriv vad som hände → valfri skärmbild (samma bildkomprimering/validering som kvittobilder). Rapporten bifogar automatiskt sidans URL, webbläsare, appversion och en kort "brödsmulesspår" av de senaste ~30 API-anropen (metod+sökväg+statuskod, aldrig request-/svarsinnehåll) — insamlat centralt i `frontend/src/api/client.ts` så ingen enskild komponent behöver instrumenteras. Ny tabell `bug_reports` (migration 9). Backend: `POST /api/bug-reports` (skapar rapporten, hastighetsbegränsad till 5/timme per användare), `POST /api/bug-reports/:id/screenshot` (bara rapportören själv får bifoga sin egen skärmbild, samma bildvalidering/normalisering som kvitton). Admin-panelen har en ny sektion "Buggrapporter": lista, visa fullständiga detaljer + skärmbild i en popup, markera löst/olöst, ta bort. Begränsat till inloggade användare (inte snabbnotegäster) eftersom gäster saknar en app-bred session att säkra uppföljningsanropet för skärmbilden mot. Tillagd i demoläges-denylistan för adminvyerna. Verifierat med nya integrationstester (rätt ägare krävs för skärmbild, endast admin kan lösa/ta bort, brödsmulor sparas korrekt) och manuellt i webbläsaren (helt flöde: skicka → visa i admin → markera löst → ta bort).
- Version 1.17.0 lägger till betalningsspårning för snabbnotor, efter en användarrapport ("går inte markera betalt efter man swishat på snabbnota") och en önskan om att se vilka som betalat. Undersökning visade att snabbnotor aldrig fick resornas motsvarande koncept (`payments`-tabellen, "Markera betald") — bara vem som bockat av vilken kvittorad, aldrig om pengarna faktiskt kommit fram. Swish exponerar ingen betalningsverifiering för en icke-handlare/privat app (kräver Swish Handel-avtal med certifikatbaserad integration), så precis som resornas befintliga betalningsflöde är detta tillitsbaserad rapportering, inte verifiering. Ny tabell `quick_tab_payments` (migration 8, samma användar/gäst-mönster som `quick_tab_claims`), ny `POST /api/quick-tabs/:id/payments` — bara ägaren (den som faktiskt tar emot Swishen) får markera någon som betald/obetald, eftersom ägaren är den enda som vet om pengarna kom fram. `personTotals` visar nu `paidAt` per person; ägaren ser en "Markera betald"-knapp per deltagare (utom sig själv), övriga ser en skrivskyddad "Betald"/"Obetald"-badge. Verifierat både med nya integrationstester (endast ägaren får markera, kaskaddata, gäst ser sin egen status) och manuellt i webbläsaren.
- Version 1.16.1 byter all synlig svensk text från "Resa"/"resor" till "Grupp"/"grupper" på användarens begäran (t.ex. "Ny resa" → "Ny grupp", "Aktiva resor" → "Aktiva grupper", felmeddelanden som "Resan finns inte" → "Gruppen finns inte"). Detta är **enbart en textändring**, inte en modell-/schemaomdöpning: `trips`-tabellen, `/api/trips`-rutterna, `tripId` och alla andra interna kodnamn är oförändrade avsiktligt, för att hålla risken låg — bara det som faktiskt syns för användaren ändrades. Expenskategorin "Resa" (🚆, menande reseutgifter/transport) är ett helt annat begrepp och lämnades orörd. Alla 10 berörda frontend-filer plus `src/server.ts` gicks igenom systematiskt med sökning efter ordgränsmatchningar (`resa|resor|resan|resorna|resans`), verifierat visuellt i webbläsare (dashboard, gruppvy, sidopanel, adminpanel).
- Version 1.16.0 är fyra separata UX-/funktionsfixar från direkt användarfeedback: (1) **Antal-fältet** för snabbnoterader var ett `type="number"`-fält som på mobilen kunde kräva nollutfyllda tvåsiffriga inmatningar (t.ex. "05") istället för att bara skriva "5" — bytt till `type="text" inputMode="numeric" pattern="[0-9]*"` (samma standardlösning som Stripe/Google använder för att undvika webbläsares inkonsekventa nummerinmatningsbeteende), verifierat manuellt i mobilemulerad webbläsare. (2) **"Bjud in"** visade QR-koden inbäddad längst ner i sidopanelen istället för tydligt fokuserad — flyttad till en riktig popup (`<dialog>`-baserad `Modal`, samma komponent som övriga dialoger använder). (3) **"Visa kvitto"** öppnade kvittobilden i en ny flik (`window.open`) — visas nu som en popup i appen, så man enkelt kommer tillbaka utan att lämna sidan. (4) **Admin-borttagning av snabbnotor**: fanns tidigare ingen väg att ta bort en snabbnota alls (inte ens för ägaren – bara stänga/öppna); ny `DELETE /api/admin/quick-tabs/:id`, admin-only (`requireAdmin`), riktig borttagning (inte papperskorg/återställning som resor har, eftersom snabbnotor är tänkta att vara kortlivade) — alla `quick_tab_*`-tabeller kaskaderar redan korrekt på `quick_tabs.id`. Ny sektion "Alla snabbnotor" i adminpanelen. Tillagd i demoläges-denylistan (kan aldrig nå riktiga snabbnotor från en demosession). Ny integrationstest täcker behörighet (endast admin, inte ens ägaren), 404 för obefintlig nota, kaskadborttagning och granskningsloggen.
- Version 1.15.7 rättar ytterligare ett kvittonamn-fel på samma Strandbryggan-kvitto, kvarstående även efter 1.15.6: `merchantName()`s poängsättning inom ett enskilt OCR-pass valde gatuadressen "Stranvägskajen 27" (14 bokstäver) före restaurangnamnet "Strandbryggan" (13 bokstäver) eftersom poängen i praktiken bara var antal bokstäver. Två strukturella signaler tillagda i stället för ett kvittospecifikt specialfall: (1) en kandidatrad som förekommer ordagrant mer än en gång bland raderna väger nu tyngre än ren bokstavslängd — ett företagsnamn skrivs ofta ut två gånger nära toppen av ett svenskt kvitto (rubrik + igen ovanför beställningsdetaljerna), medan en gatuadress bara står en gång; (2) varje siffra i kandidatraden ger nu ett litet poängavdrag, eftersom en gatuadress i princip alltid innehåller en siffra (gatunummer, postnummer) medan ett företagsnamn nästan aldrig gör det. "Strandbryggan" (skrivs ut två gånger, inga siffror) vinner nu tydligt över "Stranvägskajen 27" (en gång, två siffror).
- Version 1.15.6 rättar felaktigt kvittonamn, hittat direkt av användaren efter 1.15.5 (restaurangnamnet visades som "SAN = Servis IRS TRA SN ARS RASER VN" trots att alla kvittorader och totalsumman var helt korrekta). Orsak: `combineReceiptPasses()` valde titel genom att jämföra `merchantNameScore()` för samtliga OCR-pass (PaddleOCR-VL + den lokala Tesseract-reserven) oberoende av vilket pass som faktiskt vann på rader/totalsumma — och den poängen är i praktiken bara antal bokstäver, så en lång felläst rad från det mycket mindre tillförlitliga Tesseract-passet slog en kort korrekt "Strandbryggan" från PaddleOCR-VL. Titeln följer nu det vinnande passet, precis som belopp och datum redan gjorde (`firstValue("title")` i stället för en egen tvärgående omsortering). Bekräftat med loggarna för samma verkliga skanning (`rawText` visade korrekt "Strandbryggan" från PaddleOCR-VL) och ett nytt test.
- Version 1.15.5 rättar tre buggar som bara syntes med den nya opt-in-loggningen (`RECEIPT_OCR_DEBUG_LOG=true`) påslagen mot samma Strandbryggan-kvitto, hittade i den verkliga rå-OCR-texten från produktion (inte gissningar): (1) terminalkoden och dess registernummer stod på **samma** rad ("xCL_AT-150-E-18E #1 : 3564") med ett gement `x` i stället för separata rader som 1.15.4 antog — redan täckt av toleransen från 1.15.4, bekräftat med ett nytt test byggt på den exakta rå-texten. (2) `reuniteWrappedWords` hade ett verkligt hål: när namn och pris redan står ihop på samma rad och bara en enstaka bokstav radbryts till nästa rad ("1.00 Caesarsalla 285.00" följt av lösryckt "d"), lades bokstaven tidigare på tekniskt fel plats (efter priset) — raden slutade då inte längre på ett prisbelopp och hela kvittoraden (580 kr, "Caesarsallad" + "Tryffelpasta") föll bort helt i stället för att bara tappa en bokstav. Fragmentet klistras nu in före prisbeloppet när föregående rad redan har ett. (3) Kortterminalens dricks-rad ("Tip: 182,50 SEK") kändes inte igen som betalningsmetadata och blev en påhittad kvittorad — `tip`/`dricks`/`netto(belopp)`/`net amount` läggs nu till uteslutningslistan. Separat, orelaterad städning: loggfältet `source` var hårdkodat till `"ollama+tesseract"` även när PaddleOCR-VL faktiskt kördes (kvarleva från innan 1.12.0-bytet) — visar nu `"paddleocr+tesseract"` när PaddleOCR-VL är aktiv modell; frontend-jämförelsen som styr "lokal AI + OCR"-texten uppdaterad i samma ändring.
- Version 1.15.4 rättar den bugg 1.15.3 missade, hittad via ett verkligt kvitto (Strandbryggan, Stockholm) och docker-loggarna för samma skanning. Terminalkoden `XCL AT-150-E-18E #1` blev fortfarande en falsk kvittorad ("Cl AT-150-E-18E 41", 35,64 kr) trots `looksLikeSystemCode`-filtret från 1.15.2, eftersom PaddleOCR-VL tappade `#`-tecknet och läste en enda bokstav som gemen — filtret krävde noll gemener och gav upp helt på en enda felläst bokstav. `looksLikeSystemCode` tolererar nu upp till 25 % gemener i stället för att kräva perfekt versalläge (bekräftat reproducerbart deterministiskt, inte en gissning). Samtidigt hittades och stängdes en andra, bredare brist: en fristående 3–5-siffrig rad (t.ex. terminalkodens radnummer "3564") gjordes tidigare ovillkorligen om till ett SEK-belopp ("35,64") av `normalizeNumericGlyphs`, oavsett var på kvittot den stod — nu hoppas den omvandlingen över när raden precis ovanför ser ut som en terminal-/registerkod. Radbrytningen av "Caesarsallad"/"Tryffelpasta" på samma kvitto visade sig redan fungera korrekt (verifierat, inte antaget). Ny opt-in loggning: `RECEIPT_OCR_DEBUG_LOG=true` inkluderar en trunkerad rå-OCR-textsnutt i `ai`/`ai_verify`-raderna i `docker compose logs` för framtida felsökning utan gissningar — av som standard, eftersom kvittoinnehåll aldrig loggats sedan 1.9.0 och det förblir den avsiktliga standarden.
- Version 1.15.3 är en arkitekturgenomgång av kvittoparsern, inte ännu en enskild bugfix. Bakgrund: 1.15.1 och 1.15.2 lappade individuella metadata-mönster (bordsnummer, terminalkoder) ett i taget, men användaren fortsatte hitta nya varianter — ett tecken på att parsern saknade en grundläggande förståelse av kvittots struktur. Historikgenomgång bekräftade att AI-anropet (PaddleOCR-VL:s odekorerade `OCR:`-kommando utan tvingat JSON-schema, se v1.12.0) redan var en medveten, väl motiverad avvägning — en tidigare version körde strukturerad JSON-extraktion med Qwen3-VL (v1.5–1.11) och bytte medvetet bort från det när modellen byttes, eftersom PaddleOCR-VL är en renodlad OCR-modell och inte tränad för schemabunden strukturerad utdata. Problemet satt istället i den lokala textparsern (`src/receipt-ocr.ts`), som klassificerade rader ett mönster i taget istället för att förstå att ett kvitto alltid har samma uppbyggnad: header (butik/bord/terminal) → varor → moms/delsumma → total → betalningsdetaljer. Ny funktion `itemsSectionBounds()` hittar varusektionen strukturellt (första raden med ett kvantitetsprefixat namn eller ett prisbelopp, till första `totalWords`-träffen) och begränsar de riskfyllda korsrads-sammanslagningarna (där ett namn på en rad kan plocka upp fel pris från en närliggande rad) till den sektionen. Rader med namn+pris komplett på samma rad påverkas inte — det var aldrig den sortens rad som orsakade buggarna. Ett nytt test bevisar generaliseringen: ett helt påhittat, aldrig tidigare sett header-fält ("Löpnummer 88213-A") utesluts korrekt utan att finnas i någon nyckelordslista. Prestandaeffekt: eftersom sektionsmedvetenheten minskar antalet felklassificerade rader redan i första AI-passet, bör färre kvitton behöva den dyra andra AI-verifieringsomgången (`PADDLEOCR_ACCURATE_RETRY`) — mätbart via befintlig loggning (`aiRetried`, `balanced`, `durationMs`) i `docker compose logs`, ingen kod ändrades i själva AI-anropet eller reservvägarna.
- Version 1.15.2 förbättrar kvittotolkningen igen efter ytterligare ett verkligt kvittoexempel från användaren (Strandbryggan, Stockholm): (1) ett långt rätt-namn som radbryts på det smala kvittopapperet ("Caesarsalla" + lösryckt "d" på nästa rad, "Tryffelpast" + "a") tappade tidigare sin sista bokstav eller föll bort helt — en ny textåterföreningsfunktion (`reuniteWrappedWords`) återförenar ett kort (1–3 tecken) gement bokstavsfragment med föregående rad, oavsett om prisbeloppet hamnar ihop med fragmentet eller på en egen rad. (2) En kassaterminal-/registerkod ("XCL AT-150-E-18E #1") kunde bli en falsk kvittorad på samma sätt som "Bord 17" tidigare — filtreras nu bort via `looksLikeSystemCode` (versaler+bindestreck, inga gemener, ett mönster ingen svensk rätt har). En tidigare misstänkt bugg ("Extra" som kvittorad) visade sig vid närmare granskning vara korrekt, avsiktligt beteende (bekräftat av ett redan existerande test) och lämnades orörd.
- Version 1.15.1 rättar en kvarvarande Snabbnota-inbjudningsbugg som 1.15.0:s fix missade: varje `POST /api/quick-tabs/:id/invitations` (klick på "Bjud in") återkallade **alla** tidigare inbjudningar för notan, oavsett om de fortfarande var giltiga och oanvända. Eftersom en snabbnoteinbjudan tillåter upp till 30 användningar (`max_uses`) fanns ingen anledning att göra det — konsekvensen var att den allra första QR-koden/länken (skapad automatiskt när notan skapas, den som oftast delas direkt i en gruppchatt) slutade fungera så fort ägaren öppnade notan igen och klickade "Bjud in", även om ingen hunnit använda den än. Nu skapar varje klick bara en *ytterligare* giltig inbjudan; gamla länkar fortsätter fungera tills de går ut (14 dagar), precis som reseinbjudningar redan gjorde. Hittades genom att felsöka en verklig felrapport, inte genom kodgranskning.
- Version 1.15.0 rättar Snabbnota-återinbjudan, verifierar svensk teckenhantering, bygger klient- och serversidig kvittokomprimering, förbättrar OCR-tolkningen och lägger till ett admin-only demoläge. Ingen arkitektur byttes ut.
  - **Snabbnota igen:** ägaren kunde inte se "Bjud in"-knappen efter att ha avslutat en snabbnota (`!tab.closedAt`-villkoret dolde den helt trots att backend redan tillät en ny inbjudan när som helst) — fixat genom att ta bort villkoret. Gäster som öppnade samma inbjudningslänk igen (t.ex. efter att ha stängt fliken) skapade tidigare en helt ny `quick_tab_guests`-rad varje gång; `/api/quick-tabs/guest-join` känner nu igen en befintlig giltig gästsession för samma snabbnota och återansluter i stället för att duplicera.
  - **Svenska tecken:** verifierad end-to-end (deltagarnamn, utgiftstitlar, snabbnotenamn/produkter, `/api/users/search`) med nya regressionstester som använder Köttbullar/Räksmörgås/Öl/Blåbärspaj/Ångbåtsbryggan. Inget ASCII-filter hittades — databas, API och OCR-parsern hanterade redan å/ä/ö korrekt, men saknade testtäckning.
  - **Kvittobilder:** klienten läser bilden med `createImageBitmap` (EXIF-medveten), skalar ner till max ~3200 px långsida och komprimerar till JPEG ~0,85 innan uppladdning, med tydliga statusar ("Förbereder bild…", "Komprimerar kvitto…"). Originalbilder tillåts upp till 50 MB innan komprimering; appens hårda gräns för själva uppladdningen höjdes från 8 till 20 MB (`receiptMaximumBytes`). HEIC/HEIF upptäcks och avvisas med tydligt felmeddelande i stället för att tystnadigt misslyckas. Servern litar aldrig på klientens komprimering: varje bildkvitto normaliseras igen med Sharp (autorotation, cap vid 3000 px långsida, JPEG-kvalitet 90, metadata bortstruken) innan det lagras — detta upptäckte och stängde även en tidigare lucka där `/api/expenses/:id/receipts` inte validerade bildmått alls. Se `DEPLOYMENT.md` för Nginx/Cloudflare-gränser.
  - **OCR-tolkning:** kvittometadata som `Bord 17`, `Kassör`, `Beställning`, `Referens`, `Gäster`/`Antal gäster` och `Swish` blir inte längre kvittorader, inklusive fallet där en orelaterad summa OCR-mässigt hussamnar direkt efter en sådan rad. Rabatt-/kupongrader räknas inte längre som köpta artiklar. Nya syntetiska testkvitton (restaurang, mataffär, bar, "svår OCR") i `tests/receipt-ocr.test.mjs`.
  - **Admin-only demoläge:** ny arkitektur, se separat avsnitt nedan.
- Version 1.14.0 är en produktionshärdningsgenomgång efter React-migreringen, inriktad på säkerhet, finansiell korrekthet och drift bakom Cloudflare + Nginx Proxy Manager. Ingen arkitektur byttes ut. Ändringar: klient-IP för rate limiting läses från `CF-Connecting-IP` i stället för `X-Forwarded-For`s förstaled (det gamla sättet gick att kringgå och gjorde inloggningsspärren verkningslös bakom en proxykedja som lägger till i headern); `/api/users/search` läcker inte längre telefonnummer och `POST /api/contacts` kräver en delad resa/snabbnota; det generiska felsvaret läcker inte längre interna undantagsmeddelanden för oväntade fel; CSP:s `img-src` tillåter `blob:` (kvittoförhandsvisningen i "Ny utgift" använder `URL.createObjectURL` och blockerades annars av den strikta policyn); kvittoräkningen per utgift är nu låst i en transaktion så samtidiga uppladdningar inte kan tränga förbi femgränsen; `loadTrip` hämtar delningar/kvitton för hela resan i två frågor i stället för två frågor per utgift. Nya IDOR- och autentiseringstester lades till i `tests/server.integration.test.mjs` (utomstående användare nekas trip/kvitto/snabbnota-åtkomst, inloggningsspärren testas end-to-end, betalningsflödet har nu testtäckning) samt namngivna finansiella gränsfall i `tests/split.test.mjs`. Frontend: redigering av en procent-/andelsdelad utgift tappade tidigare tyst delningstypen (belopp förblev korrekta men metoden byttes till "exakt") — fixat; formulär för utgift/resa/betalning/snabbnota/person visar nu ett spärrat "sparar…"-läge under insändning för att undvika dubbletter vid dåligt mobilnät; snabbnotagäster vars session gått ut fastnade tidigare utan väg tillbaka efter ett 401-svar — fixat; sökracet i deltagarsök (stale response kunde skriva över ett nyare sökresultat) fixat med en request-räknare. Ny `DEPLOYMENT.md` (topologi, proxy-tillitsmodell, uppdatering/backup/återställning/rollback) och `PUBLIC_LAUNCH_CHECKLIST.md` (vad som saknas före en bredare lansering).
- Version 1.13.0 ersätter den tidigare globala vanilla-JavaScript-klienten med React 19, Vite och strikt TypeScript. Alla befintliga arbetsflöden använder samma server-API: konton och inbjudningar, dashboard och vänner, resor, deltagare, utgifter och fyra delningssätt, kategorier, kvitton/OCR, saldon och betalningar, snabbnota med gästläge/SSE/Swish-länk, statistik, administration och versionsvisning. Docker bygger frontend och backend i samma build stage och slutcontainern är fortfarande en enda read-only Node-app. Databasschema, OCR-tjänster, Compose-portar, volymer och miljövariabler ändrades inte.
- Version 1.12.0 byter den lokala dokumentmodellen till PaddleOCR-VL 1.6 via dess officiella GGUF-distribution och en intern, versionspinnad llama.cpp CUDA-server. Modellen använder det dokumenterade `OCR:`-kommandot utan framtvingat JSON-schema. Parsern hanterar både traditionella artikelrader och PaddleOCR-resultat där flera produktnamn följs av ett separat prisblock, samt totalrubrik och totalbelopp på skilda rader. Modellfilerna hämtas atomiskt till en beständig Unraid-katalog. Tesseract och alla säkra reservvägar behålls.
- Version 1.11.0 byter till den officiella `qwen3-vl:4b-instruct-q4_K_M`, eftersom thinking-varianten kan returnera hundratals tokens i `message.thinking` men lämna `message.content` tomt för bilder. Reserv-OCR använder en naturligare gråskalebild och behandlar inte längre största artikelpriset som totalsumma när en oläslig totalrad faktiskt finns. `thinking_only` loggas separat utan att modellens resonemang eller kvittodata sparas.
- Version 1.10.0 begränsar Qwen3-VL:s kvittosvar till 768 tokens, stänger uttryckligen av thinking-läge och skickar en mindre AI-bild utan att minska Tesseracts upplösning. Detta förhindrar minutlånga svar som annars fortsatte till timeout. Säkra loggar visar nu avslutsorsak, prompt- och svarstokens samt modellens laddnings- och inferenstid utan att logga kvittodata.
- Version 1.9.0 rätar ut fotograferade papperskvitton mot mörk bakgrund och kombinerar alternativa läsningar genom att välja radbelopp som får kvittot att summera exakt. Heltalsbelopp före `SEK`, prioriterade beställningsdatum, OCR-varianter av antalmarkören `x` och styckpris gånger antal hanteras deterministiskt. En exakt lokal tolkning avbryter den långsamma AI-körningen. Säkra loggar och klientstatus visar om Ollama användes, tog timeout, saknade modell, inte kunde nås eller returnerade ogiltigt svar; kvittobild och OCR-text loggas aldrig.
- Version 1.8.0 byter huvudmodell från GLM-OCR 0,9B till Qwen3-VL 4B Q4_K_M. Modellen returnerar ett validerat kvittoschema och använder 8K kontext. Tesseract kör fortfarande parallellt, medan ofullständiga eller matematiskt inkonsekventa AI-resultat automatiskt får en andra kontroll. GTX 1080 Ti har 11 GB VRAM; Compose tillåter därför två samtidiga modellförfrågningar med Flash Attention och q8-KV-cache, medan två Tesseract-arbetare nyttjar CPU:n utan en global kö. Modellen är cirka 3,3 GB; första modellhämtningen tar därför längre tid än tidigare.
- Version 1.7.0 beskär automatiskt bort iPhone-förhandsvisning, verktygsfält och tomma bildmarginaler innan kvittot skalas upp. GLM-OCR använder modellens dokumenterade textigenkänningsläge och kör parallellt med Tesseract, med 45 sekunders konfigurerbar timeout (`OLLAMA_OCR_TIMEOUT_MS`). Parsern bevarar kompakta antal som `3x` och `7x` och filtrerar betalningsrader.
- Version 1.6.0 lägger till antalval på snabbnoter, deterministisk öresfördelning per vald mängd och en Swish-knapp till snabbnotans skapare. Befintliga rader migreras säkert som antal 1.
- Compose använder `runtime: nvidia` för kompatibilitet med Unraid Compose Manager i stället för det nyare `gpus`-fältet.
- Version 1.5.0 lägger till helt lokal GLM-OCR via Ollama, adaptiv flerpass-OCR, bildnormalisering och bättre hantering av radbrytningar och teckenfel i belopp.
- Kamera och bildbibliotek har separata knappar så att användaren alltid kan välja en befintlig bild om Chrome-kameran inte fungerar på enheten.
- PaddleOCR körs endast på det interna Compose-nätverket med `ghcr.io/ggml-org/llama.cpp:server-cuda-b9570`; appen faller automatiskt tillbaka till Tesseract om modellen inte är tillgänglig.
- Version 1.4.0 innehåller förbättrad svensk kvittoradstolkning och kontolösa snabbnotegäster.
- Setup-sessionens HTTP-svar skickas först efter att PostgreSQL-transaktionen har committats, vilket förhindrar en sporadisk 401 direkt efter första installationen.
- PostgreSQL-integrationstest, TypeScript, lint och övriga tester är gröna för denna version.
- Docker Compose ändrades inte i version 1.4.0.

## Regler för löpande uppdatering

Uppdatera detta dokument när någon av följande saker ändras:

- appversion eller högsta migrationsnummer;
- tjänster, images, portar, volymer, backuper eller Unraid-instruktioner;
- autentisering, roller, inbjudningar eller åtkomstmodell;
- ekonomiska beräkningar eller datalivscykel;
- större funktioner, borttagna funktioner eller kända begränsningar;
- test-, CI-, release- eller rollbackflöde.

Skriv bara verifierade fakta om det aktuella repot. Kortlivade arbetsanteckningar och spekulationer hör inte hemma här.
